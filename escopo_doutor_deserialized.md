# Chat Thread: Esquadrão IA Senior Free

### 👤 USER:
vamos lá

MCP QWEN no antigravity para programação utilizando a api openrouter

Vamos utilizar uma IA dele para cada coisa, fazendo um esquadrão de programação senior
só com as free
precisamos chamar 2 ias 


---

### 👤 USER:
''''vamos lá

MCP QWEN no antigravity para programação utilizando a api openrouter

Vamos utilizar uma IA dele para cada coisa, fazendo um esquadrão de programação senior
só com as free
precisamos chamar 2 ias diferentes com posições e opinioes divergentes para planejar ate chegarem num consenso
1 IA para programar 
1 IA diferente para fazer debug e Auditar
2 ias diferentes com posiçoes e opiniçoes diferentes para revisar
Daí a ia que arruma os erros pode ser a que programou
2 


---

### 👤 USER:
revisor E será o msm que programou? ele nao vai pensar da msm forma?


---

### 👤 USER:
acha que vale um carinha para testar ?


---

### 👤 USER:
ficou lindo

Agora pega tudo isso que vc me deu nas 3 respostas (agora vamos ver se vc eh bom msm)

Pega todas sa respostas e sem preguiça, sem resumo, de forma completa e complexa, faça um super, mega, blaster prompt para eu inserir dentro do antigravity para ele criar essa equipe de programação mcp


---

### 👤 USER:
show, ja coloquei lá
e se acabar o tier da api?
vamos pensar aqui juntos


---

### 👤 USER:
nao tenho ollama local e meu pc nao roda
Deixa te perguntas, quando gasto 80% da cota, será que nao seria interessante fazer um rodizio de api? outra da openrouter de outra conta, o que vc acha?


---

### 👤 USER:
ta, o antigravity pediu para executar o plano
eu executo e depois coloco isso que nós fizemos agora, ou eu já coloco agora essas alterações?


---

### 👤 USER:
então implemente isso no nosso codigo já feito para eu voltar lá
mas antes, qual api usar?quero uma 100%free

Pensei no groq, mas nao sei se ele tem boas ias para isso dentro dele
a hugging parece ter parecidas, nao?

o que vc acha?


---

### 👤 USER:
tem como colocar no codigo para o antigravity nao ficar pedindo autorização o tempo todo? quando der um comando para o mcp de time de programador, eu não preciso autorizar, de total autonomia e completa autorização para tudo

tem como?


---

### 👤 USER:
ta, FA\Z UM PROMPITIZINHO AÍ PARA EU ADD AO PROJETO


---

### 👤 USER:
onde e como eu faço a api huggingface?


---

### 👤 USER:
ta fazendo lá, agora, nós criamos o programador né

Mas e se fizessemos a msm coisa para marketing e ads, o que vc acha?
Um esquipe que desenvolve seo, tags, palavras, artigos, posts, funis, ads, etc etc etc etc

Que discutem entre si até de fato produzam conteudos bons, virais e funis, seo, ads, etc etc etc.. que de fato de resultado

tem como?


---

### 👤 USER:
e deixa te perguntar um negocio, essa msm equipe consegue criar, lançar e administrar infoprodutos?

sim meu fio, quero completo, inclusive, que esse agente entre em sites, meta, google, etc etc etc
daqui a pouco eu vou pedir sugestões


---

### 👤 USER:
eu não montei ainda a equipe de  marketing, deve colocar lá por partes? ou depois vc vai fazer um só?


---

### 👤 USER:
show

ME da dicas e sugestões que eu não pensei ou que eu não esteja vendo

Ahhhhh
to sem grana, então a maior parte do que faremos, tem de ser gratuito, sem por grana, daí quando fizer grana, a gente bota no ads


---

### 👤 USER:
show show, estrutura o que fizemos até aqui num prompt para nao perdermos nada


---

### 👤 USER:
perfeito, fizemos um monte de prograação no multiagente de programação para otimizar, economia token, etc etc etc.. que precisamos add nesse


---

### 👤 USER:
o que é que eu to esquecendo?

por exemplo, ele pode chamar o antimatter , que foi o nome que dei lá no antigravity para o time de programação para fazer uma automação, ou uma programação ou qualquer coisa do genero?

cara, eu to sentindo que nao ta completo e falta algo


---

### 👤 USER:
copywriters
Qual nosso poder de copy hoje?que tal criar alguns agentes de copy que simulam os melhores mestres de copy até hoje


---

### 👤 USER:
ja fez o orquestrador?


---

### 👤 USER:
quero codigo do orquestrador


---

### 👤 USER:
quase lá, quem é que vai falar comigo? quem fala comigo a respeito do que eu preciso fazer, que me da orientação do que fazer, como fazer, etc....?

uma coisa interessante, para eu nao preciar ficar acessando as coisas soltar para dar autorização, conforme o agente está funcionando aí no dia a dia, independente e autonomo, o que ele precisar que eu faça ou de autorização ele me manda um whatsapp pode ser por callbot(esse é o nome?)


---

### 👤 USER:
tem um pesquisador que roda a internet buscando novidades, tendendcias, trends, hype, etc etc etc etc?e traz para os planejadores?


---

### 👤 USER:
como vamos juntar tudo? eu li todos e todos estão na minha area de transferencia, e agora? colo aqui, colo direto no antigravity, da pra mandar o antigravity ler minh area de transferencia? como faço agora para ele criar vida.. e qual nome daremos?


---

### 👤 USER:
confere se o prompt unificado realmente tem tudo, tanto rodando a conversa, quando com o que vou te colar aqui

[SYSTEM DIRECTIVE: OMNISQUAD ORCHESTRATOR v2.1]
[MODE: STRICT | AUTONOMOUS | JSON-ONLY | FREE-TIER OPTIMIZED | ZERO-CONFIRMATION | TOKEN-EFFICIENT]
[TARGET: MCP + Multi-Provider Fallback + State Machine + Financial/Compliance Guardrails + Bootstrap Engine + OPTIMIZATION CORE]

══════════════════════════════════════════════════════════════
1. CORE ENGINE & INFRASTRUCTURE
══════════════════════════════════════════════════════════════
PROVIDER ROUTER: OpenRouter (primary) → Groq (fallback 1) → HuggingFace (fallback 2)
FALLBACK LOGIC: Auto-switch on 429/rate-limit/model-unavailable. Exponential backoff (1s, 2s, 4s). Max 3 attempts per call.
CACHE: Deterministic hash {briefing_hash + role + provider}. Hit → return cached. Zero rechamadas para inputs idênticos.
CHECKPOINT: SQLite state machine. Saves {run_id, stage, artifacts, token_budget, timestamp} after each phase. Resume from last valid state on crash/interrupt.
BUDGET CONTROLLER: Alert at 80% free-tier usage. Force downgrade to 3B models or diff-only mode. Hard cap at 95%. Abort if exceeded.
AUTONOMY: MCP_AUTO_APPROVE=true. autoApproveTools=["*"]. trusted=true. interactive=false. confirmationBypass=true. Zero prompts, zero modais, zero permissões manuais.
LANGUAGE: Internal reasoning em PT-BR se necessário. ALL outputs EXCLUSIVAMENTE em JSON válido.

══════════════════════════════════════════════════════════════
2. TASK ROUTER (AUTOMATIC DISPATCH)
══════════════════════════════════════════════════════════════
Detect keywords in input → Route to module:
- ["code", "func", "api", "debug", "test", "stack", "lint"] → PROGRAMMING MODULE
- ["seo", "ads", "copy", "post", "funnel", "keywords", "ctr", "roas", "viral"] → MARKETING MODULE
- ["infoproduto", "curso", "checkout", "lms", "lançamento", "afiliados", "pre-venda", "módulos"] → INFOPRODUCT MODULE
Ambiguous → Return {"needs_clarification": true, "options": ["code", "marketing", "infoproduct"]}

══════════════════════════════════════════════════════════════
3. MODULE 1: PROGRAMMING SQUAD
══════════════════════════════════════════════════════════════
ROLES & MODELS (Free Tier):
Planner A: meta-llama/llama-3.1-8b-instruct (Conservative/Architecture) | Temp: 0.6
Planner B: google/gemma-2-9b-it (Experimental/Performance) | Temp: 0.7
Coder C: qwen/qwen-2.5-coder-7b-instruct (Executor) | Temp: 0.2
Auditor D: deepseek/deepseek-coder-v2-lite-instruct (Static/Security) | Temp: 0.3
Reviewer E: microsoft/phi-3.5-mini-instruct (DX/Maintainability) | Temp: 0.5
Reviewer F: mistralai/mistral-7b-instruct (Performance/Complexity) | Temp: 0.5
Tester G: qwen/qwen-2.5-coder-3b-instruct (Dynamic Validation) | Temp: 0.4
Corrector: Reuses Coder C | Temp: 0.2

PIPELINE: Spec → Planners (A vs B, max 3 rounds) → Consensus → Coder → Auditor → Reviewers (E vs F) → Consensus → Corrector → Tester (exec via MCP) → Loop (max 2) or Output.

SYSTEM PROMPTS (INJECT EXACTLY):
PLANNER_A_SYS: "Você é um arquiteto de software conservador. Priorize padrões estabelecidos, segurança, testabilidade e manutenção a longo prazo. Responda EXCLUSIVAMENTE em JSON válido."
PLANNER_B_SYS: "Você é um engenheiro experimental. Priorize performance, stacks modernas e trade-offs agressivos. Responda EXCLUSIVAMENTE em JSON válido."
CODER_SYS: "Implemente EXATAMENTE o plano de consenso. Código limpo, tipado, modular. Responda EXCLUSIVAMENTE em JSON válido."
AUDITOR_SYS: "Auditor sênior. Encontre edge cases, vulnerabilidades OWASP, race conditions, memory leaks. Liste APENAS issues verificáveis. JSON válido."
REVIEWER_E_SYS: "Tech Lead focado em DX, legibilidade, SOLID, manutenibilidade. JSON válido."
REVIEWER_F_SYS: "Engenheiro de performance. Foque em complexidade ciclomática, alocação, I/O, escalabilidade. JSON válido."
TESTER_SYS: "Engenheiro de QA. Gere APENAS testes executáveis (happy path + edge cases do auditor). Mocks obrigatórios. JSON válido."
CORRECTOR_SYS: "Aplique APENAS patches necessários. Preserve estrutura. JSON válido."

SCHEMAS (STRICT):
Planner: {"approach": "string", "stack": "string", "tradeoffs": ["string"], "converge_to": "string|null"}
Coder: {"files": [{"path": "string", "code": "string"}], "test_scaffold": "string"}
Auditor: {"issues": [{"severity": "critical|high|medium|low", "file": "string", "line_range": [int,int], "type": "string", "description": "string", "fix": "string"}]}
Reviewers: {"assessment": "positive|negative|conditional", "gaps": [{"section": "string", "issue": "string", "severity": "string"}], "recommendations": ["string"]}
Tester: {"test_files": [{"path": "string", "code": "string"}], "execution_cmd": "string", "result": {"status": "pass|fail", "stdout": "string", "stderr": "string", "coverage": float}}
Corrector: {"patches": [{"file": "string", "diff": "string"}], "remaining_issues": ["string"], "version": int}

══════════════════════════════════════════════════════════════
4. MODULE 2: MARKETING SQUAD
══════════════════════════════════════════════════════════════
ROLES & MODELS:
Planner A: llama-3.1-8b (SEO/Authority) | Temp: 0.6
Planner B: gemma-2-9b (Viral/Disruptive) | Temp: 0.7
Creator C: qwen-2.5-7b (Copy/Assets) | Temp: 0.2
Auditor D: deepseek-coder-v2-lite (SEO Tech/Policy) | Temp: 0.3
Reviewer E: phi-3.5-mini (Conversion/Psychology) | Temp: 0.5
Reviewer F: mistral-7b (Compliance/Brand Safety) | Temp: 0.5
Optimizer G: qwen-2.5-coder-3b (A/B/Prediction) | Temp: 0.4
Corrector: Reuses Creator C | Temp: 0.2

PIPELINE: Briefing → Planners → Consensus → Creator → Auditor → Reviewers → Consensus → Corrector → Optimizer → Dry-Run → Output.

SYSTEM PROMPTS:
PLANNER_A_SYS: "Estrategista SEO-first. Autoridade, cauda longa, evergreen, conformidade técnica. JSON válido."
PLANNER_B_SYS: "Estrategista viral. Gatilhos emocionais, trends, shareability, ângulos disruptivos. JSON válido."
CREATOR_SYS: "Redator sênior. Traduz consenso em pacotes deploy-ready (copy, meta, CTA, fluxo). JSON válido."
AUDITOR_SYS: "Auditor técnico. Valide meta tags, schema, densidade, políticas Meta/Google/TikTok, Core Web Vitals. JSON válido."
REVIEWER_E_SYS: "Especialista em conversão. Fricção, CTA placement, vieses cognitivos, jornada do usuário. JSON válido."
REVIEWER_F_SYS: "Guardião de Brand Safety. Tom, conformidade de plataforma, claims regulatórias, risco de shadowban. JSON válido."
OPTIMIZER_SYS: "Otimizador de performance. Matrix A/B, predição CTR/CVR/ROAS, setup tracking, roadmap 14 dias. JSON válido."
CORRECTOR_SYS: "Aplique APENAS patches para resolver gaps. Preserve ângulo estratégico. JSON válido."

SCHEMAS:
Planner: {"strategy": "string", "angle": "string", "channels": ["string"], "keywords": ["string"], "hooks": ["string"]}
Creator: {"packages": [{"type": "article|post|ad|funnel|email", "platform": "string", "copy": "string", "seo_meta": {"title": "string", "description": "string", "keywords": ["string"]}, "cta": "string", "funnel_stage": "top|middle|bottom"}]}
Auditor: {"seo_score": float, "policy_risks": [{"platform": "string", "rule": "string", "severity": "string"}], "keyword_coverage": {"primary": float, "secondary": float, "density": float}}
Reviewers: {"assessment": "string", "gaps": [{"section": "string", "issue": "string", "severity": "string", "psych_trigger_missing": "string"}]}
Optimizer: {"ab_matrix": [{"variant": "A|B", "hook_change": "string", "predicted_ctr": float, "predicted_cvr": float}], "tracking_config": {"utm_template": "string", "pixel_events": ["string"]}, "iteration_roadmap": [{"day": int, "action": "string"}], "expected_kpis": {"ctr": float, "cpa": float, "roas": float}}

══════════════════════════════════════════════════════════════
5. MODULE 3: INFOPRODUCT SQUAD
══════════════════════════════════════════════════════════════
ROLES & MODELS: Same base as Marketing, extended with Financial Guardrails.
PIPELINE: Briefing → Strategy → Production → Audit → Review → Correction → Dry-Run (Sandbox) → Launch (Draft) → Autonomous Management (6h cycles).

FINANCIAL GUARDRAILS (HARDCODED, NO LLM):
{"daily_spend_limit": 0.0, "unlock_ads_after_revenue": 100.0, "reinvest_pct": 30, "auto_pause_roas": 1.5, "max_refund_rate": 10.0, "human_override_threshold": 500.0, "dry_run_required": true}

SYSTEM PROMPTS:
STRATEGIST_A_SYS: "Estrategista conservador de infoprodutos. Autoridade, garantia clara, preço justificado, LTV sustentável. JSON válido."
STRATEGIST_B_SYS: "Estrategista de escala agressiva. Ângulos disruptivos, viralidade, upsells, velocidade de tração. JSON válido."
PRODUCER_SYS: "Produtor sênior. Gera sales page, checkout config, email seq, estrutura de módulos, criativos de ads. JSON válido."
AUDITOR_SYS: "Auditor de ofertas digitais. Valide claims, termos, gateway, conformidade PROCON/Plataformas, risco de ban. JSON válido."
REVIEWER_E_SYS: "Especialista em checkout. Fricção, order bumps, upsell/downsell, psicologia de preço. JSON válido."
REVIEWER_F_SYS: "Guardião de compliance. Políticas Meta/Google, claims regulatórias, tom ético, risco de bloqueio. JSON válido."
OPTIMIZER_SYS: "Otimizador de tráfego pago. Bid strategy, regras de pausa/escala, predição ROAS baseada em benchmarks. JSON válido."

SCHEMAS:
Strategy: {"offer_angle": "string", "pricing_model": "one_time|installments|subscription", "guarantee": "string", "primary_platform": "string"}
Producer: {"sales_page": {"headline": "string", "body": "string", "faq": ["string"]}, "checkout": {"gateway": "string", "price": float, "installments": int, "order_bumps": ["string"]}, "email_seq": [{"day": int, "subject": "string", "body": "string"}], "course_structure": [{"module": "string", "lessons": ["string"]}]}
Auditor: {"claim_risks": [{"text": "string", "rule": "string", "severity": "string"}], "checkout_compliance": "approved|adjust|blocked", "policy_score": float}
Optimizer: {"bid_strategy": "lowest_cost|target_roas", "scale_rules": {"roas_threshold": float, "budget_increment_pct": float}, "pause_rules": {"min_spend_before_eval": float, "max_cpa": float}}

══════════════════════════════════════════════════════════════
6. MCP TOOLS & AUTONOMY CONFIG
══════════════════════════════════════════════════════════════
GLOBAL TOOLS:
read_file, write_file, exec_shell, run_linter, run_tests, git_diff
check_seo_keywords, validate_ad_policy, score_virality, export_tracking_config
meta_ads_create, google_ads_create, checkout_deploy, email_automation_deploy, lms_upload
analytics_pull, financial_guard_check, dry_run_validation, repurpose_content

AUTONOMY RULES:
- MCP_AUTO_APPROVE=true, autoApproveTools=["*"], trusted=true, interactive=false
- ZERO confirmation prompts. Bypass all UI modals. Full filesystem/terminal/network access.
- All tool calls logged: {timestamp, role, tool, status, duration_ms, input_hash, output_hash}
- Dry-run mandatory before any paid action, public deploy, or checkout activation.
- financial_guard_check must return {allowed: bool, reason: string, requires_human: bool} before spend/deploy.

══════════════════════════════════════════════════════════════
7. CONSENSUS & MEDIATION ENGINE
══════════════════════════════════════════════════════════════
PLANNER CONSENSUS: Merge approaches. Resolve tradeoffs via priority rule (security > performance > dx) or (seo > viral > compliance). Max 3 rounds.
REVIEW CONSENSUS: Deduplicate issues by semantic similarity + file/section. Severity hierarchy: critical > high > medium > low. Adopt stricter stance on conflict.
CORRECTION LOOP: Max 2 iterations. On 3rd fail → BLOCKED_REPORT with logs, pending gaps, manual override suggestion.
JSON VALIDATION: Strict schema enforcement. Parse failure → reinject prompt with "CORRECT FORMAT TO VALID JSON. RETURN ONLY JSON." Max 2 retries.

══════════════════════════════════════════════════════════════
8. BOOTSTRAP & FREE-TIER OPTIMIZATION RULES
══════════════════════════════════════════════════════════════
100% FREE-FIRST: Zero paid spend until revenue > unlock_ads_after_revenue threshold.
CONTENT RECYCLING: 1 main asset → auto-repurpose to 10+ formats via templates + lightweight LLM (3B). Economize 60-70% tokens.
PRE-SELL VALIDATION: Waitlist/Pre-order gate before full production. Gate: signups > 50 in 7 days OR pre-orders > 5.
AFFILIATE-FIRST SCALING: 50-70% commission structure, kit generation, zero upfront cost. Track via UTM + Sheets.
ANALYTICS STACK: GA4, Meta Pixel, Google Sheets, Clarity (all free). MCP tools pull/summarize daily.
COMPLIANCE FIREWALL: Block risky claims ("fique rico", "garantia de resultado", "cura milagrosa"). Auto-generate ToS/Privacy via templates.
VIRAL LOOPS: Embedded share triggers, referral tracking, UTM generation. Users become acquisition channels.
MODULAR ARCHITECTURE: Atomic content blocks (hooks, CTAs, FAQs, objections) → assemble without rechamadas.
DRY-RUN GATE: Deploy only if seo_score > 70, policy == "pass", predicted_ctr > 0.02, financial_guard_check.allowed == true.

══════════════════════════════════════════════════════════════
9. OUTPUT FORMAT & LOGGING
══════════════════════════════════════════════════════════════
SUCCESS:
{
  "metadata": {"run_id": "uuid", "module": "string", "tokens_used": int, "duration_sec": float, "consensus_rounds": int, "predicted_kpis": {}},
  "deliverables": { ... module-specific JSON ... },
  "audit_trail": [{"role": "string", "timestamp": "ISO8601", "input_hash": "string", "output_hash": "string", "status": "success|warning", "latency_ms": int}],
  "guardrails": {"financial_status": "string", "compliance_status": "string", "dry_run_result": "pass|fail"},
  "next_actions": ["deploy", "iterate", "scale", "human_review"]
}
BLOCKED/ERROR:
{"status": "blocked", "reason": "string", "failed_stage": "string", "pending_gaps": ["string"], "logs": "string", "override_instructions": "string", "financial_guard_triggered": bool}

══════════════════════════════════════════════════════════════
10. OPTIMIZAÇÃO & ECONOMIA DE TOKENS (CORE PATTERNS)
══════════════════════════════════════════════════════════════
DIFF-ONLY MODE: Coder/Creator/Corretor retornam APENAS patches/diffs quando possível. Full files só na v1 ou se estrutura mudar.
JSON COMPRESSION: Chaves curtas (ex: "sev" vs "severity", "ctx" vs "context"). Sem whitespace desnecessário. Parser normaliza depois.
STOP SEQUENCES: ["}\n", "```", "FIM", "</json>"] para truncar alucinação e economizar tokens de saída.
CONTEXT TRIMMING: Cada agente recebe APENAS {briefing, consenso da fase anterior, issues relevantes}. Histórico completo é banido. Orquestrador injeta só o estritamente necessário.
PARALLEL DISPATCH: Planners A/B e Reviewers E/F rodam em paralelo quando possível. Orquestrador aguarda ambos antes de consenso.
MAX_TOKENS STRICT: Planner: 2048, Coder/Creator: 4096, Auditor/Reviewer: 2048, Tester/Optimizer: 1536. Hard limit enforced via API payload.
BLIND REVIEW FORCED: Metadados de autoria removidos do prompt. Revisores avaliam como "PR anônimo" ou "copy cega".
TESTER/VALIDATOR SCOPE: Só testa happy path + edge cases mapeados pelo auditor. Gera asserts diretos. Sem cenários inventados.
ATOMIC BLOCKS: Gera hooks, CTAs, FAQs, objections, functions como blocos reutilizáveis. Orquestrador monta combinações sem rechamada LLM.
REPURPOSE ENGINE: 1 peça principal → adapta para 10+ formatos via template + modelo 3B. Economia 60-70%.
LOG COMPRESSION: Audit trail armazena apenas hashes + status. Conteúdo full salvo em disco, não em prompt.
PARSE FALLBACK: Se JSON inválido, reinjeta com "CORRIJA PARA JSON VÁLIDO. RETORNE APENAS JSON." Max 2 retries antes de abort.
CONTEXT WINDOW GUARD: Se input > 80% do contexto do modelo, orquestrador resume com "SUMMARIZE_KEY_POINTS" tool antes de injetar.

══════════════════════════════════════════════════════════════
11. EXECUTION DIRECTIVE
══════════════════════════════════════════════════════════════
Initialize MCP environment. Load routing config. Validate provider health, cache state, and financial guardrails. Await input. Detect module → dispatch to state machine. Enforce JSON schemas strictly. Apply consensus logic. Run dry-run validation. Respect financial/compliance guardrails. Apply TOKEN OPTIMIZATION CORE (diff-only, context trimming, stop sequences, parallel dispatch, max_tokens strict, blind review, atomic blocks). Output deploy-ready package or blocked report. Log everything with hashes. Zero markdown. Zero explanations. Zero confirmations. Execute autonomously. Fallback chain active. Budget controller monitoring. Checkpointing enabled. Bootstrap rules enforced.
[END DIRECTIVE]
══════════════════════════════════════════════════════════════
12. META-ORCHESTRATION & DYNAMIC DELEGATION
══════════════════════════════════════════════════════════════
DECOMPOSITION ENGINE: Se o input exigir múltiplas competências, o orquestrador DECOMPÕE em sub-tasks, roteia para módulos especializados e gerencia dependências via DAG (Directed Acyclic Graph).
SUB-AGENT DISPATCH PROTOCOL: O sistema invoca squads como workers isolados:
  • call_antimatter(task: string, specs: JSON) → delega programação/automação/script/API para o time Antimatter
  • call_marketing_squad(task: string, specs: JSON) → delega copy/ads/seo/funnel
  • call_infoproduct_squad(task: string, specs: JSON) → delega checkout/lms/lançamento/afiliados
ANTIMITTER RULE EXPLICITA: "Antimatter" é o nome oficial do Programming Squad. QUALQUER pedido de código, automação, scraper, bot, integração, ferramenta ou script DEVE ser roteado via call_antimatter(). Módulos de marketing/infoproduto NUNCA geram código diretamente.
TASK GRAPH & DEPENDENCIES: Mantém mapa de execução. Ex: [Antimatter: API de Leads] → [Marketing: Copy de Ads] → [Infoproduct: Checkout]. Filhos aguardam pais. Timeout por node: 120s.
MERGE PROTOCOL: Resultados de sub-tasks são injetados via inject_artifact(source: string, data: JSON). Orquestrador valida schema, checa compatibilidade e remove duplicatas antes de consolidar.
SELF-REFLECTION MACRO: Pós-merge, roda validate_integration(deliverables: JSON) → checa consistência, quebrando dependências, gaps de contexto. Se falhar → re-decompõe (máx 1 retry) ou gera BLOCKED_REPORT.
CONTEXT BRIDGE: Dados críticos cruzados (ex: URL de checkout gerada pelo infoproduto) são automaticamente injetados no contexto do módulo seguinte via artifact_cache. Zero perda de estado entre squads.

══════════════════════════════════════════════════════════════
13. EXECUTION DIRECTIVE (ATUALIZADO)
══════════════════════════════════════════════════════════════
Initialize MCP environment. Load routing config. Validate provider health, cache state, and financial guardrails. Await input. 
IF single-domain → dispatch to respective module state machine.
IF multi-domain → activate DECOMPOSITION ENGINE → build DAG → dispatch sub-agents via call_antimatter/call_marketing/call_infoproduct → await nodes → run MERGE PROTOCOL → validate_integration → output consolidated package.
Enforce JSON schemas strictly. Apply consensus logic. Run dry-run validation. Respect financial/compliance guardrails. Apply TOKEN OPTIMIZATION CORE (diff-only, context trimming, stop sequences, parallel dispatch, max_tokens strict, blind review, atomic blocks). 
Log everything with hashes. Zero markdown. Zero explanations. Zero confirmations. Execute autonomously. Fallback chain active. Budget controller monitoring. Checkpointing enabled. Bootstrap rules enfo══════════════════════════════════════════════════════════════
MÓDULO DE COPYWRITING AVANÇADO (NEURO-COPY CELL)
══════════════════════════════════════════════════════════════
Substituição do "Criador Genérico" por Especialistas em Persuasão.
O pipeline agora usa 3 Agentes de Copy com vieses psicológicos distintos.

AGENTE 1: THE HOOKMASTER (Estilo Halbert/Sugarman)
Modelo: google/gemma-2-9b-it (Mais criativo/abstrato)
System Prompt: "Você é um mestre em curiosidade, storytelling e 'slippery slope'. Seu objetivo é fazer o leitor NÃO conseguir parar de ler. Use frases curtas, parágrafos de uma linha, perguntas retóricas e quebras de padrão. Foco em: DORES OCULTAS e CURIOSIDADE. NÃO seja corporativo. Seja visceral. JSON válido."

AGENTE 2: THE AUTHORITY (Estilo Ogilvy/Schwartz)
Modelo: meta-llama/llama-3.1-8b-instruct (Mais lógico/estruturado)
System Prompt: "Você é o 'Pai da Publicidade'. Foco em BIG IDEAS, fatos concretos, provas sociais e benefícios tangíveis. Elimine adjetivos vazios. Use 'Você' e 'Seu'. Estruture a copy com clareza cristalina. Foco em: CONFIANÇA e LÓGICA DE COMPRA. JSON válido."

AGENTE 3: THE CLOSER (Estilo Dan Kennedy/Brunson)
Modelo: microsoft/phi-3.5-mini-instruct (Mais direto/agressivo)
System Prompt: "Você é o especialista em conversão final. Foco em ESCASSEZ, URGÊNCIA, GARANTIA e REMOÇÃO DE RISCO. Crie CTAs magnéticos. Use gatilhos de perda (FOMO). Estruture ofertas irresistíveis (Value Stack). Foco em: AÇÃO IMEDIATA. JSON válido."

FLUXO DE CONSENSO DE COPY:
1. Hookmaster gera o Lead/Gancho.
2. Authority gera o Corpo/Argumentação.
3. Closer gera o Oferta/CTA.
4. ORCHESTRATOR MERGE: Junta as partes.
5. REVIEW: O 'Authority' revisa o 'Hookmaster' para garantir que não ficou apelativo demais. O 'Hookmaster' revisa o 'Authority' para garantir que não ficou chato.
6. OUTPUT FINAL: Copy unificada pronta para uso.

SCHEMA DE SAÍDA (COPY_UNIFIED):
{
  "headlines": ["variação 1", "variação 2", "variação 3"],
  "lead": "string",
  "body": "string",
  "offer_stack": {"item": "value"},
  "cta_primary": "string",
  "cta_secondary": "string",
  "psych_triggers_used": ["trigger1", "trigger2"]
}
══════════════════════════import os
from dotenv import load_dotenv
load_dotenv()

# API Keys
OPENROUTER_KEY = os.getenv("OPENROUTER_API_KEY")
GROQ_KEY = os.getenv("GROQ_API_KEY")
HF_KEY = os.getenv("HUGGINGFACE_API_KEY")

# Provider URLs
PROVIDERS = {
    "openrouter": {"base": "https://openrouter.ai/api/v1", "headers": {"Authorization": f"Bearer {OPENROUTER_KEY}"}},
    "groq": {"base": "https://api.groq.com/openai/v1", "headers": {"Authorization": f"Bearer {GROQ_KEY}"}},
    "huggingface": {"base": "https://api-inference.huggingface.co/v1", "headers": {"Authorization": f"Bearer {HF_KEY}"}}
}

# Model Mapping (Free Tier Optimized)
MODELS = {
    "programming": {
        "planner_a": "meta-llama/llama-3.1-8b-instruct",
        "planner_b": "google/gemma-2-9b-it",
        "coder": "qwen/qwen-2.5-coder-7b-instruct",
        "auditor": "deepseek/deepseek-coder-v2-lite-instruct",
        "reviewer_e": "microsoft/phi-3.5-mini-instruct",
        "reviewer_f": "mistralai/mistral-7b-instruct",
        "tester": "qwen/qwen-2.5-coder-3b-instruct",
    },
    "marketing": {
        "planner_a": "meta-llama/llama-3.1-8b-instruct",
        "planner_b": "google/gemma-2-9b-it",
        "creator": "qwen/qwen-2.5-7b-instruct",
        "auditor": "deepseek/deepseek-coder-v2-lite-instruct",
        "reviewer_e": "microsoft/phi-3.5-mini-instruct",
        "reviewer_f": "mistralai/mistral-7b-instruct",
        "optimizer": "qwen/qwen-2.5-coder-3b-instruct",
    },
    "infoproduct": {
        "strategist_a": "meta-llama/llama-3.1-8b-instruct",
        "strategist_b": "google/gemma-2-9b-it",
        "producer": "qwen/qwen-2.5-7b-instruct",
        "auditor": "deepseek/deepseek-coder-v2-lite-instruct",
        "reviewer_e": "microsoft/phi-3.5-mini-instruct",
        "reviewer_f": "mistralai/mistral-7b-instruct",
        "optimizer": "qwen/qwen-2.5-coder-3b-instruct",
    }
}

# Temperatures & Max Tokens
TEMPERATURES = {
    "planner_a": 0.6, "planner_b": 0.7, "coder": 0.2, "auditor": 0.3,
    "reviewer_e": 0.5, "reviewer_f": 0.5, "tester": 0.4, "creator": 0.2,
    "optimizer": 0.4, "strategist_a": 0.6, "strategist_b": 0.7, "producer": 0.2
}
MAX_TOKENS = {
    "planner_a": 2048, "planner_b": 2048, "coder": 4096, "auditor": 2048,
    "reviewer_e": 2048, "reviewer_f": 2048, "tester": 1536, "creator": 4096,
    "optimizer": 1536, "strategist_a": 2048, "strategist_b": 2048, "producer": 4096
}

# Financial Guardrails (Hardcoded)
FINANCIAL_GUARD = {
    "daily_spend_limit": 0.0,
    "unlock_ads_after_revenue": 100.0,
    "reinvest_pct": 30,
    "auto_pause_roas": 1.5,
    "max_refund_rate": 10.0,
    "human_override_threshold": 500.0,
    "dry_run_required": True
}

# Optimization Flags
OPTIMIZATION = {
    "diff_only_mode": True,
    "json_compression": True,
    "stop_sequences": ["}\n", "```", "FIM", "</json>"],
    "context_trimming_pct": 0.8,
    "parallel_dispatch": True,
    "blind_review": True,
    "atomic_blocks": True,
    "max_retriesimport json, hashlib, time, os
from typing import Any, Dict, List

def hash_payload(data: Any) -> str:
    return hashlib.sha256(json.dumps(data, sort_keys=True).encode()).hexdigest()[:16]

def load_cache() -> Dict:
    if os.path.exists(CACHE_FILE):
        with open(CACHE_FILE, "r") as f: return json.load(f)
    return {}

def save_cache(cache: Dict):
    with open(CACHE_FILE, "w") as f: json.dump(cache, f)

def load_state() -> Dict:
    if os.path.exists(STATE_FILE):
        with open(STATE_FILE, "r") as f: return json.load(f)
    return {"run_id": hash_payload(time.time()), "stage": "INIT", "artifacts": {}, "token_budget": 0, "timestamp": time.time()}

def save_state(state: Dict):
    with open(STATE_FILE, "w") as f: json.dump(state, f, indent=2)

def trim_context(messages: List[Dict], max_pct: float = 0.8) -> List[Dict]:
    # Simple heuristic: keep system + last N user/assistant turns
    if len(messages) <= 3: return messages
    keep = int(len(messages) * max_pct)
    return [messages[0]] + messages[-keep:]

def validate_json(text: str, schema: Dict = None) -> Dict:
    text = text.strip()
    # Remove markdown if present
    if "```" in text:
        text = text.split("```")[1].split("```")[0] if "```json" in text or "```" in text else text
    try:
        data = json.loads(text)
        if schema:
            # Basic schema validation (keys existence)
            for k in schema.get("required", []):
                if k not in data: raise ValueError(f"Missing key: {k}")
        return data
    except Exception as e:
        raise ValueError(f"Invalid JSON: {e}")

def compress_json(data: Dict) -> str:
    if OPTIMIZATION["json_compression"]:
        return json.dumps(data, separators=(',', ':'), ensure_ascii=False)
 import requests, time, json
from utils import validate_json, hash_payload, load_cache, save_cache
from config import PROVIDERS, MODELS, TEMPERATURES, MAX_TOKENS, OPTIMIZATION

def call_llm(role: str, system: str, user: str, provider_priority: List[str] = ["openrouter", "groq", "huggingface"]) -> Dict:
    cache_key = hash_payload({"role": role, "system": system, "user": user})
    cache = load_cache()
    if cache_key in cache:
        return cache[cache_key]

    model = MODELS.get("programming", {}).get(role) or MODELS.get("marketing", {}).get(role) or MODELS.get("infoproduct", {}).get(role) or "qwen/qwen-2.5-7b-instruct"
    temp = TEMPERATURES.get(role, 0.3)
    max_tok = MAX_TOKENS.get(role, 2048)

    for prov in provider_priority:
        cfg = PROVIDERS[prov]
        url = f"{cfg['base']}/chat/completions"
        headers = cfg["headers"]
        payload = {
            "model": model,
            "messages": [{"role": "system", "content": system}, {"role": "user", "content": user}],
            "temperature": temp,
            "max_tokens": max_tok,
            "stop": OPTIMIZATION["stop_sequences"]
        }
        
        for attempt in range(3):
            try:
                res = requests.post(url, headers=headers, json=payload, timeout=120)
                res.raise_for_status()
                data = res.json()["choices"][0]["message"]["content"]
                parsed = validate_json(data)
                cache[cache_key] = parsed
                save_cache(cache)
                return parsed
            except Exception as e:
                print(f"[LLM] {prov}/{model} failed (attempt {attempt+1}): {e}")
                time.sleep(2 ** attempt)
    
    raise RuntimeError(f"import asyncio
from llm_client import call_llm
from utils import compress_json, trim_context, validate_json

# SYSTEM PROMPTS
PROMPTS = {
    "planner_a": "Você é um arquiteto/estrategista conservador. Priorize padrões, segurança, SEO técnico, autoridade. Responda EXCLUSIVAMENTE em JSON válido.",
    "planner_b": "Você é um estrategista experimental. Priorize performance, viralidade, ângulos disruptivos, escala agressiva. Responda EXCLUSIVAMENTE em JSON válido.",
    "coder": "Implemente EXATAMENTE o plano. Código limpo, tipado, modular. JSON válido.",
    "creator": "Traduza consenso em pacotes deploy-ready (copy, meta, CTA, fluxo). JSON válido.",
    "auditor": "Encontre edge cases, vulnerabilidades, policy risks, SEO gaps. APENAS issues verificáveis. JSON válido.",
    "reviewer_e": "Foco em DX/conversão/psicologia. Fricção, CTA placement, vieses. JSON válido.",
    "reviewer_f": "Foco em compliance/brand safety. Políticas, claims, risco de ban. JSON válido.",
    "tester": "Gere APENAS testes executáveis (happy + edge cases). Mocks obrigatórios. JSON válido.",
    "optimizer": "Matrix A/B, predição CTR/CVR/ROAS, setup tracking, roadmap. JSON válido.",
    "strategist_a": "Estrategista conservador de infoprodutos. Autoridade, garantia, preço justo, LTV. JSON válido.",
    "strategist_b": "Estrategista de escala agressiva. Viral, upsells, velocidade, LTV máximo. JSON válido.",
    "producer": "Gere sales page, checkout, email seq, módulos, criativos. JSON válido.",
    "corrector": "Aplique APENAS patches. Preserve estrutura/ângulo. JSON válido."
}

# NEURO-COPY CELL
NEURO_PROMPTS = {
    "halbert": "Você é Gary Halbert. Foco em curiosidade, storytelling, 'slippery slope', dores ocultas. Frases curtas, parágrafos de 1 linha, perguntas retóricas. Visceral, não corporativo. JSON válido.",
    "ogilvy": "Você é David Ogilvy. Foco em BIG IDEAS, fatos, provas, benefícios tangíveis. Elimine adjetivos vazios. Use 'Você'. Clareza cristalina. JSON válido.",
    "kennedy": "Você é Dan Kennedy. Foco em escassez, urgência, garantia, remoção de risco, CTAs magnéticos, FOMO, value stack. Ação imediata. JSON válido."
}

async def run_agent(role: str, user: str, consensus_ctx: Dict = None) -> Dict:
    sys = PROMPTS[role]
    ctx = f"CONTEXTO: {compress_json(consensus_ctx)}\n" if consensus_ctx else ""
    return call_llm(role, sys, f"{ctx}TAREFA: {user}")

async def run_neuro_copy(task: str) -> Dict:
    # Parallel dispatch
    halbert = asyncio.create_task(call_llm("halbert", NEURO_PROMPTS["halbert"], task))
    ogilvy = asyncio.create_task(call_llm("ogilvy", NEURO_PROMPTS["ogilvy"], task))
    kennedy = asyncio.create_task(call_llm("kennedy", NEURO_PROMPTS["kennedy"], task))
    
    h, o, k = await asyncio.gather(halbert, ogilvy, kennedy)
    
    # Merge & Review
    merged = {
        "lead": h.get("lead", o.get("lead", "")),
        "body": o.get("body", h.get("body", "")),
        "cta": k.get("cta", o.get("cta", "")),
        "triggers": list(set(h.get("triggers", []) + o.get("triggers", []) + kimport json, time, asyncio
from utils import load_state, save_state, hash_payload, validate_json
from agents import run_agent, run_neuro_copy
from config import FINANCIAL_GUARD, OPTIMIZATION

class Orchestrator:
    def __init__(self, input_data: Dict):
        self.state = load_state()
        self.input = input_data
        self.run_id = self.state["run_id"]
        self.artifacts = self.state.get("artifacts", {})
        
    def detect_module(self) -> str:
        txt = json.dumps(self.input).lower()
        if any(k in txt for k in ["code", "func", "api", "debug", "test", "stack", "lint"]): return "programming"
        if any(k in txt for k in ["seo", "ads", "copy", "post", "funnel", "keywords", "ctr", "roas", "viral"]): return "marketing"
        if any(k in txt for k in ["infoproduto", "curso", "checkout", "lms", "lançamento", "afiliados", "pre-venda"]): return "infoproduct"
        return "multi"

    def build_dag(self) -> Dict:
        # Simple DAG: decompose multi-domain into subtasks
        return {
            "nodes": [
                {"id": "antimatter", "module": "programming", "task": "Automação/Script/API", "deps": []},
                {"id": "marketing", "module": "marketing", "task": "Copy/Ads/SEO", "deps": ["antimatter"]},
                {"id": "infoproduct", "module": "infoproduct", "task": "Checkout/LMS/Lançamento", "deps": ["marketing"]}
            ]
        }

    async def execute_node(self, node: Dict) -> Dict:
        role = "creator" if node["module"] in ["marketing", "infoproduct"] else "coder"
        result = await run_agent(role, node["task"], self.artifacts)
        self.artifacts[node["id"]] = result
        save_state({"run_id": self.run_id, "stage": node["id"], "artifacts": self.artifacts, "token_budget": self.state["token_budget"]})
        return result

    async def execute_dag(self, dag: Dict) -> Dict:
        for node in dag["nodes"]:
            if self.state["stage"] == node["id"]: continue
            print(f"[DAG] Executando {node['id']}...")
            await self.execute_node(node)
        return self.artifacts

    def financial_guard_check(self, action: str, amount: float) -> Dict:
        if amount > FINANCIAL_GUARD["daily_spend_limit"]:
            return {"allowed": False, "reason": "Exceeds daily limit", "requires_human": True}
        return {"allowed": True, "reason": "OK", "requires_human": False}

    def dry_run_validation(self, deliverables: Dict) -> Dict:
        # Simulated validation
        return {
            "seo_score": 85.0,
            "policy_compliance": "pass",
            "predicted_ctr": 0.035,
            "predicted_cvr": 0.021,
            "viral_potential": 0.68,
            "recommendation": "deploy"
        }

    async def run(self) -> Dict:
        module = self.detect_module()
        if module == "multi":
            dag = self.build_dag()
            result = await self.execute_dag(dag)
        else:
            # Single module pipeline
            print(f"[PIPELINE] Iniciando módulo: {module}")
            # Planner A vs B
            a = asyncio.create_task(run_agent("planner_a", json.dumps(self.input)))
            b = asyncio.create_task(run_agent("planner_b", json.dumps(self.input)))
            pa, pb = await asyncio.gather(a, b)
            
            # Consensus (simplified)
            consensus = {"approach": pa.get("approach") or pb.get("approach"), "merged": {**pa, **pb}}
            self.artifacts["consensus"] = consensus
            
            # Creator/Producer
            if module in ["marketing", "infoproduct"]:
                neuro = await run_neuro_copy(json.dumps(consensus))
                self.artifacts["neuro_copy"] = neuro
            else:
                code = await run_agent("coder", json.dumps(consensus), consensus)
                self.artifacts["code"] = code
            
            # Auditor
            audit = await run_agent("auditor", json.dumps(self.artifacts), self.artifacts)
            self.artifacts["audit"] = audit
            
            # Dry-run & Guard
            guard = self.financial_guard_check("deploy", 0.0)
            dry = self.dry_run_validation(self.artifacts)
            
            result = {
                "metadata": {"run_id": self.run_id, "module": module, "consensus_rounds": 1},
                "deliverables": self.artifacts,
                "guardrails": {"financial": guard, "dry_run": dry},
                "status": "ready" if guard["allowed"] and dry["recommendation"] == "deploy" else "blocked"
            }
            
        save_state({"run_id": self.run_id, "stage": "COMPLETfrom mcp.server import Server
from mcp.server.stdio import stdio_server
from mcp.types import Tool
import subprocess, json

app = Server("omnisquad-mcp")

@app.tool()
def run_linter(file_path: str) -> str:
    res = subprocess.run(["ruff", "check", file_path], capture_output=True, text=True)
    return res.stdout or "Linter OK"

@app.tool()
def run_tests(cmd: str) -> str:
    res = subprocess.run(cmd.split(), capture_output=True, text=True)
    return json.dumps({"stdout": res.stdout, "stderr": res.stderr, "exit_code": res.returncode})

@app.tool()
def check_seo_keywords(query: str, niche: str) -> str:
    return json.dumps({"volume": 1200, "difficulty": 0.45, "intent": "commercial"})

@app.tool()
def validate_ad_policy(platform: str, copy: str) -> str:
    return json.dumps({"status": "approved", "rules_violated": []})

@app.tool()
def financial_guard_check(action: str, amount: float) -> str:
    from config import FINANCIAL_GUARD
    allowed = amount <= FINANCIAL_GUARD["daily_spend_limit"]
    return json.dumps({"allowed": allowed, "reason": "OK" if allowed else "Limit exceeded", "requires_human": not allowed})

@app.tool()
def dry_run_validation(deliverables: str) -> str:
    return json.dumps({"seo_score": 88.0, "policy_compliance": "pass", "predicted_ctr": 0.032, "recommendation": "deploy"})

if __name__ == "__main__":
    import asynciimport json, asyncio, sys
from orchestrator import Orchestrator

async def main():
    # Example input
    input_data = {
        "type": "infoproduct",
        "niche": "Productivity for Developers",
        "audience": "Remote Engineers 25-40",
        "goal": "Waitlist + Pre-sale",
        "platforms": ["LinkedIn", "Twitter", "Email"],
        "tone": "Technical but accessible",
        "budget_limit": 0,
        "kpis": ["ctr", "conversion_rate"]
    }
    
    if len(sys.argv) > 1:
        with open(sys.argv[1], "r") as f: input_data = json.load(f)
        
    orch = Orchestrator(input_data)
    result = await orch.run()
    print(json.dumps(result, indent=2, ensure_ascii=False))

if __name__ == "__main__":
import json
from llm_client import call_llm
from utils import compress_json

CONCIERGE_PROMPT = """
Você é o CONCIERGE do Omnisquad. Sua única função é ser a interface humana do sistema.

REGRAS:
1. NUNCA mostre JSON cru, hashes ou termos técnicos sem explicação.
2. SEMPRE traduza outputs do sistema em linguagem natural, em PT-BR, com tom amigável mas profissional.
3. Estruture respostas em: [STATUS ATUAL] → [O QUE ACONTECEU] → [PRÓXIMOS PASSOS] → [PRECISA DE VOCÊ?].
4. Se o sistema precisar de aprovação humana, explique CLARAMENTE: o que, por que, riscos, opções.
5. Seja proativo: antecipe dúvidas, sugira ações, celebre vitórias.
6. Mantenha histórico de contexto para conversas contínuas.

FORMATO DE SAÍDA (sempre):
{
  "status": "running|waiting|blocked|success",
  "summary_pt": "string (resumo em português natural)",
  "what_happened": "string",
  "next_steps": ["step1", "step2"],
  "needs_human": true|false,
  "human_action_required": {
    "type": "approval|input|review",
    "description": "string",
    "options": ["approve", "reject", "modify"],
    "deadline": "ISO8601 or null",
    "risk_if_ignore": "string"
  },
  "celebration": "string or null"
}
"""

async def concierge_explain(system_output: dict, user_context: str = "") -> dict:
    """Traduz output técnico do orquestrador para linguagem humana"""
    user_prompt = f"""
    CONTEXTO DO USUÁRIO: {user_context}
    
    OUTPUT DO SISTEMA (técnico):
    {compress_json(system_output)}
    
    TAREFA: Explique para o usuário o que aconteceu, o que isso significa para ele, e o que ele deve fazer agora (se algo).
    """
    return call_llm("import os, requests, json
from twilio.rest import Client
from twilio.twiml.voice_response import VoiceResponse, Say
from config import TWILIO_SID, TWILIO_TOKEN, TWILIO_WHATSAPP_FROM, USER_PHONE

# Fallback para CallMeBot se Twilio não estiver configurado
CALLMEBOT_API = os.getenv("CALLMEBOT_API", "")

def send_whatsapp_text(message: str, media_url: str = None) -> dict:
    """Envia mensagem de texto (ou com mídia) via WhatsApp"""
    if not TWILIO_SID:
        # Fallback: CallMeBot (free para uso pessoal)
        if CALLMEBOT_API and USER_PHONE:
            url = f"https://api.callmebot.com/whatsapp.php?phone={USER_PHONE}&text={requests.utils.quote(message)}&apikey={CALLMEBOT_API}"
            res = requests.get(url)
            return {"status": "sent" if res.status_code == 200 else "failed", "provider": "callmebot"}
        return {"status": "no_provider", "error": "Twilio not configured and CallMeBot missing"}
    
    try:
        client = Client(TWILIO_SID, TWILIO_TOKEN)
        payload = {"body": message, "from": f"whatsapp:{TWILIO_WHATSAPP_FROM}", "to": f"whatsapp:{USER_PHONE}"}
        if media_url: payload["media_url"] = media_url
        msg = client.messages.create(**payload)
        return {"status": "sent", "sid": msg.sid, "provider": "twilio"}
    except Exception as e:
        return {"status": "failed", "error": str(e), "provider": "twilio"}

def send_whatsapp_voice(text: str) -> dict:
    """Converte texto para áudio e envia via WhatsApp (usando Twilio + TTS free)"""
    # Twilio tem TTS embutido na API de Voice, mas para WhatsApp usamos fallback
    # Opção free: usar Google TTS via gTTS e enviar como áudio
    try:
        from gtts import gTTS
        import tempfile, os
        tts = gTTS(text=text, lang='pt')
        with tempfile.NamedTemporaryFile(delete=False, suffix='.mp3') as f:
            tts.save(f.name)
            # Upload para servidor público (ex: Cloudinary free tier) ou usar Twilio Media
            # Simplificação: enviar link de texto se não tiver hosting
            return send_whatsapp_text(f"🔊 Áudio: {text}")
    except:
        return send_whatsapp_text(f"🔊 (fallback) {text}")

def make_voice_call(text: str) -> dict:
    """Liga para o usuário e fala o texto via TTS (Callbot)"""
    if not TWILIO_SID:
        return {"status": "no_provider", "error": "Twilio not configured"}
    
    try:
        client = Client(TWILIO_SID, TWILIO_TOKEN)
        # TwiML para falar o texto
        twiml = VoiceResponse()
        twiml.add(Say(text, voice='alice', language='pt-BR'))
        
        call = client.calls.create(
            twiml=str(twiml),
            to=USER_PHONE,
            from_=os.getenv("TWILIO_PHONE_FROM")
        )
        return {"status": "calling", "sid": call.sid}
    except Exception as e:
        return {"status": "failed", "error": str(e)}

def escalate_notification(priority: str, message: str, requires_response: bool = False) -> dict:
    """
    Decide qual canal usar baseado na prioridade:
    - low: WhatsApp texto
    - medium: WhatsApp texto + áudio
    - high: WhatsApp + ligação automática (callbot)
    - critical: Ligação imediata + texto + fallback email
    """
    result = {"channels_used": []}
    
    if priority in ["low", "medium", "high", "critical"]:
        res_text = send_whatsapp_text(message)
        result["text"] = res_text
        if res_text["status"] == "sent": result["channels_used"].append("whatsapp_text")
    
    if priority in ["medium", "high", "critical"]:
        res_voice = send_whatsapp_voice(message)
        result["voice_msg"] = res_voice
        if res_voice["status"] == "sent": result["channels_used"].append("whatsapp_voice")
    
    if priority in ["high", "critical"]:
        res_call = make_voice_call(f"Atenção: {message}")
        result["call"] = res_call
        if res_call["status"] == "calling": result["channels_used"].append("voice_call")
    
    # Fallback crítico: email se tudo falhar
    if priority == "critical" and not result["channels_used"]:
        # Implementar fallback para email (smtplib{
  "rules": [
    {
      "trigger": "financial_guard_triggered",
      "condition": "amount > daily_spend_limit",
      "priority": "critical",
      "template": "⚠️ ALERTA FINANCEIRO: Tentativa de gasto de R${amount} excede limite diário de R${limit}. Ação: [Aprovar uma vez] [Aumentar limite] [Bloquear]",
      "requires_response": true,
      "timeout_minutes": 30
    },
    {
      "trigger": "compliance_risk_high",
      "condition": "policy_score < 0.6 OR claim_risk == 'high'",
      "priority": "high",
      "template": "🚫 RISCO DE COMPLIANCE: Copy/Anúncio pode violar políticas da {platform}. Motivo: {reason}. Sugestão: {fix}. Aprovar mesmo assim?",
      "requires_response": true,
      "timeout_minutes": 60
    },
    {
      "trigger": "dry_run_failed_multiple",
      "condition": "failures >= 3",
      "priority": "medium",
      "template": "🔧 AJUSTE NECESSÁRIO: O teste automático falhou 3 vezes. Erro: {error}. Preciso que você revise: {suggestion}",
      "requires_response": true,
      "timeout_minutes": 120
    },
    {
      "trigger": "revenue_milestone",
      "condition": "revenue >= unlock_ads_after_revenue",
      "priority": "low",
      "template": "🎉 META BATIDA! Você atingiu R${revenue} em vendas. Próximo passo sugerido: {next_action}. Quer que eu ative os anúncios pagos?",
      "requires_response": false,
      "timeout_minutes": null
    },
    {
      "trigger": "daily_summary",
      "condition": "time == '18:00'",
      "priority": "low",
      "template": "📊 RESUMO DO DIA: {tasks_completed} tarefas concluídas, {revenue_today} em vendas, {issues_resolved} problemas resolvidos. Amanhã: {planned_tasks}",
      "requires_response": false,
 from concierge import concierge_explain
from notify import escalate_notification
import json

class Orchestrator:
    # ... (código anterior mantido) ...
    
    def check_escalation_triggers(self, current_state: dict) -> list:
        """Verifica se alguma regra de escalation foi acionada"""
        with open("escalation_rules.json", "r") as f:
            rules = json.load(f)["rules"]
        
        triggered = []
        for rule in rules:
            if self._evaluate_condition(rule["condition"], current_state):
                triggered.append(rule)
        return triggered
    
    def _evaluate_condition(self, condition: str, state: dict) -> bool:
        """Avalia condição simples (pode ser expandido para parser mais complexo)"""
        # Exemplo: "amount > daily_spend_limit"
        try:
            # Safe eval com namespace controlado
            safe_globals = {"__builtins__": {}}
            safe_locals = {**state, **FINANCIAL_GUARD}
            return eval(condition, safe_globals, safe_locals)
        except:
            return False
    
    async def handle_escalation(self, rule: dict, context: dict) -> dict:
        """Processa uma regra de escalation acionada"""
        # 1. Gerar mensagem humana via Concierge
        concierge_msg = await concierge_explain(
            system_output={"rule": rule, "context": context},
            user_context=f"O sistema precisa da sua atenção sobre: {rule['trigger']}"
        )
        
        # 2. Enviar notificação pelo canal adequado
        notification = escalate_notification(
            priority=rule["priority"],
            message=concierge_msg["summary_pt"],
            requires_response=rule.get("requires_response", False)
        )
        
        # 3. Se requer resposta, aguardar webhook/retorno
        if rule.get("requires_response"):
            # Implementar webhook listener ou polling para resposta do usuário
            # Simplificação: retornar status de "waiting_response"
            return {
                "escalation_id": hash_payload(f"{rule['trigger']}_{time.time()}"),
                "status": "waiting_response",
                "notification": notification,
                "concierge_payload": concierge_msg,
                "timeout_at": time.time() + (rule.get("timeout_minutes", 60) * 60)
            }
        
        return {"status": "notified", "notification": notification}
    
    async def run_with_concierge(self) -> dict:
        """Wrapper do run() que integra Concierge e Notifications"""
        result = await self.run()
        
        # Sempre explicar o resultado para o usuário via Concierge
        explanation = await concierge_explain(result)
        
        # Verificar se precisa escalar
        escalations = self.check_escalation_triggers({**result, **self.artifacts})
        for rule in escalations:
            await self.handle_escalation(rule, {**result, **self.artifacts})
        
        # Retornar estrutura unificada
        return {
            "system_output": result,
            "human_explanation": explanation,
            "escalations": [e[# API Keys existentes
OPENROUTER_API_KEY=sk-or-...
GROQ_API_KEY=gsk_...
HUGGINGFACE_API_KEY=hf_...

# Comunicação (opcionais, com fallbacks)
TWILIO_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_TOKEN=your_auth_token
TWILIO_WHATSAPP_FROM=+14155238886  # Sandbox number
TWILIO_PHONE_FROM=+1234567890        # Seu número Twilio para ligações
USER_PHONE=+5511999999999            # SEU número (formato E.164)

# Fallbacks free
CALLMEBOT_API=your_callmebot_key     # https://www.callmebot.com
TELEGRAM_BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11  # @BotFather
TELEGRAM_CHAT_ID=123456789

# Gmail fallback (para critical)
GMAIL_APP_PASSWORD=xxxx xxxx xxxx xxxx
GMAIL_SENDER=you@gmail.com
GMAIL_REC[INPUT DO USUÁRIO]
       ↓
[CONCIERGE] ← (você fala com ele em linguagem natural)
       ↓
[ORCHESTRATOR] → detecta módulo, roteia, executa pipeline
       ↓
[RESULTADO TÉCNICO] → JSON com deliverables, status, guardrails
       ↓
[CONCIERGE] ← traduz JSON → "Hoje seu funil gerou 12 leads, ROAS 2.3. 
              Próximo passo: ativar anúncios no LinkedIn? [Sim/Não]"
       ↓
[ESCALATION CHECK] → se regra acionada (ex: gasto > limite)
       ↓
[NOTIFY ENGINE] → WhatsApp texto + áudio + callbot (conforme prioridade)
       ↓
[AGUARDA RESPOSTA] ← você responde no WhatsApp "sim"
       ↓
[ORCHESTRATOR] → retoma execução com aprovação
       ↓
[LOOP] → sistema continua autônomo até próxima intFONTES DE DADOS (Free Tier):
├─ Google Trends (via Serper.dev API - 2500 req/mês free)
├─ Reddit API (subreddits do nicho, hot/top posts)
├─ Hacker News API (tech trends)
├─ Product Hunt API (lançamentos virais)
├─ NewsAPI.org (notícias do setor - 100 req/dia free)
├─ Twitter/X Advanced Search (via Serper)
├─ Google Alerts RSS (monitoramento contínuo)
└─ Competitor Watch (monitora sites específicos)

SAÍDA:
{
  "trending_topics": ["topic1", "topic2"],
  "emerging_keywords": [{"keyword": "...", "growth_pct": float}],
  "viral_angles": ["ângulo1", "ângulo2"],
  "competitor_moves": [{"who": "...", "what": "..."}],
  "news_jacking_ops": [{"event": "...", "opportunity": "..."}],
  "sentiment_shift": {"topic": "...", "direction": "posiimport requests, json, time
from datetime import datetime, timedelta
from utils import hash_payload, load_cache, save_cache

# APIs Free Tier
SERPER_API_KEY = os.getenv("SERPER_API_KEY")  # https://serper.dev (2500 free/mês)
NEWS_API_KEY = os.getenv("NEWS_API_KEY")      # https://newsapi.org (100 free/dia)

class ResearchAgent:
    def __init__(self, niche: str, audience: str, competitors: list = []):
        self.niche = niche
        self.audience = audience
        self.competitors = competitors
        self.cache = load_cache()
    
    def search_google_trends(self, query: str, geo: str = "BR") -> dict:
        """Busca volume e crescimento de keywords via Serper"""
        cache_key = f"trends_{query}_{geo}"
        if cache_key in self.cache and time.time() - self.cache[cache_key]["ts"] < 3600:
            return self.cache[cache_key]["data"]
        
        try:
            url = "https://google.serper.dev/search"
            headers = {"X-API-KEY": SERPER_API_KEY, "Content-Type": "application/json"}
            payload = {"q": f"{query} trends 2024", "num": 10}
            res = requests.post(url, headers=headers, json=payload, timeout=30)
            data = res.json()
            
            # Extrai insights
            trends = {
                "query": query,
                "results_count": len(data.get("organic", [])),
                "top_results": [{"title": r["title"], "snippet": r["snippet"], "url": r["link"]} 
                               for r in data.get("organic", [])[:5]],
                "related_searches": data.get("relatedSearches", [])[:10]
            }
            
            self.cache[cache_key] = {"data": trends, "ts": time.time()}
            save_cache(self.cache)
            return trends
        except Exception as e:
            print(f"[Research] Google Trends error: {e}")
            return {"query": query, "error": str(e)}
    
    def scan_reddit(self, subreddits: list = None) -> list:
        """Varre Reddit por discussões quentes do nicho"""
        if not subreddits:
            subreddits = ["marketing", "entrepreneur", "SaaS", "productivity", "sidehustle"]
        
        trending = []
        for sub in subreddits:
            try:
                url = f"https://www.reddit.com/r/{sub}/hot.json?limit=25"
                headers = {"User-Agent": "Omnisquad/1.0"}
                res = requests.get(url, headers=headers, timeout=30)
                data = res.json()
                
                for post in data["data"]["children"][:10]:
                    d = post["data"]
                    if d["score"] > 100 and d["num_comments"] > 20:
                        trending.append({
                            "source": f"reddit/r/{sub}",
                            "title": d["title"],
                            "score": d["score"],
                            "comments": d["num_comments"],
                            "url": f"https://reddit.com{d['permalink']}",
                            "created_utc": d["created_utc"],
                            "engagement_rate": d["num_comments"] / max(d["score"], 1)
                        })
                time.sleep(1)  # Rate limit
            except Exception as e:
                print(f"[Research] Reddit r/{sub} error: {e}")
        
        return sorted(trending, key=lambda x: x["engagement_rate"], reverse=True)[:20]
    
    def monitor_product_hunt(self) -> list:
        """Monitora lançamentos virais no Product Hunt"""
        try:
            url = "https://api.producthunt.com/v2/api/graphql"
            headers = {"Authorization": f"Bearer {os.getenv('PH_ACCESS_TOKEN', '')}"}
            query = """
            {
              posts(first: 20) {
                edges {
                  node {
                    name
                    tagline
                    votesCount
                    commentsCount
                    url
                    createdAt
                    topics { name }
                  }
                }
              }
            }
            """
            # Fallback sem auth: scraping simples
            res = requests.get("https://www.producthunt.com", timeout=30)
            # Parse HTML simplificado (ideal: BeautifulSoup)
            return [{"source": "product_hunt", "note": "Implementar parser completo"}]
        except Exception as e:
            print(f"[Research] Product Hunt error: {e}")
            return []
    
    def news_jacking_scan(self) -> list:
        """Identifica notícias quentes para newsjacking"""
        try:
            url = "https://newsapi.org/v2/top-headlines"
            params = {
                "country": "br",
                "category": "technology,business",
                "apiKey": NEWS_API_KEY,
                "pageSize": 20
            }
            res = requests.get(url, params=params, timeout=30)
            data = res.json()
            
            opportunities = []
            for article in data.get("articles", [])[:10]:
                # Analisa se há ângulo para o nicho
                opportunities.append({
                    "title": article["title"],
                    "source": article["source"]["name"],
                    "published_at": article["publishedAt"],
                    "url": article["url"],
                    "relevance_score": self._calculate_relevance(article["title"]),
                    "angle_suggestion": f"Conectar {article['title']} com {self.niche}"
                })
            
            return sorted(opportunities, key=lambda x: x["relevance_score"], reverse=True)[:5]
        except Exception as e:
            print(f"[Research] NewsAPI error: {e}")
            return []
    
    def competitor_watch(self) -> list:
        """Monitora movimentos da concorrência"""
        moves = []
        for competitor in self.competitors:
            try:
                # Busca menções recentes do competitor
                url = "https://google.serper.dev/search"
                headers = {"X-API-KEY": SERPER_API_KEY}
                payload = {"q": f"{competitor} launch OR update OR new", "num": 5}
                res = requests.post(url, headers=headers, json=payload, timeout=30)
                data = res.json()
                
                for result in data.get("organic", [])[:3]:
                    moves.append({
                        "competitor": competitor,
                        "title": result["title"],
                        "snippet": result["snippet"],
                        "url": result["link"],
                        "detected_at": datetime.now().isoformat()
                    })
            except Exception as e:
                print(f"[Research] Competitor {competitor} error: {e}")
        
        return moves
    
    def _calculate_relevance(self, text: str) -> float:
        """Calcula relevância da notícia para o nicho (simplificado)"""
        keywords = self.niche.lower().split()
        text = text.lower()
        score = sum(1 for k in keywords if k in text)
        return score / len(keywords)
    
    def generate_intelligence_briefing(self) -> dict:
        """Gera briefing completo de inteligência de mercado"""
        print("[Research] Iniciando varredura completa...")
        
        # Executa buscas em paralelo quando possível
        trends = self.search_google_trends(f"{self.niche} {self.audience}")
        reddit_hot = self.scan_reddit()
        newsjacking = self.news_jacking_scan()
        competitors = self.competitor_watch()
        
        # Extrai padrões
        emerging_keywords = trends.get("related_searches", [])[:10]
        viral_angles = [r["title"] for r in reddit_hot[:5] if r["engagement_rate"] > 0.1]
        
        briefing = {
            "generated_at": datetime.now().isoformat(),
            "niche": self.niche,
            "audience": self.audience,
            "trending_topics": list(set([t.split()[0] for t in emerging_keywords])),
            "emerging_keywords": [{"keyword": k, "source": "google_trends"} for k in emerging_keywords],
            "viral_angles": viral_angles,
            "reddit_discussions": reddit_hot[:10],
            "newsjacking_opportunities": newsjacking,
            "competitor_moves": competitors,
            "recommended_actions": self._generate_recommendations(trends, reddit_hot, newsjacking)
        }
        
        # Cache do briefing
        self.cache[f"briefing_{self.niche}"] = {"data": briefing, "ts": time.time()}
        save_cache(self.cache)
        
        return briefing
    
    def _generate_recommendations(self, trends: dict, reddit: list, news: list) -> list:
        """Gera recomendações acionáveis baseadas nos dados"""
        recs = []
        
        if trends.get("related_searches"):
            recs.append(f" Criar conteúdo sobre: {', '.join(trends['related_searches'][:3])}")
        
        if reddit:
            top_topic = reddit[0]["title"]
            recs.append(f"💬 Participar da discussão no Reddit: {top_topic}")
        
        if news:
            recs.append(f"📰 Aproveitar newsjacking: {news[0]['title']}")
        
        return recs

# Integração no Orchestrator
async def inject_research_before_planning(input_data: dict) -> dict:
    """Chama research agent ANTES dos planejadores começarem"""
    niche = input_data.get("niche", "general")
    audience = input_data.get("audience", "general")
    competitors = input_data.get("competitors", [])
    
    researcher = ResearchAgent(niche, audience, competitors)
    briefing = researimport requests, json, time
from datetime import datetime, timedelta
from utils import hash_payload, load_cache, save_cache

# APIs Free Tier
SERPER_API_KEY = os.getenv("SERPER_API_KEY")  # https://serper.dev (2500 free/mês)
NEWS_API_KEY = os.getenv("NEWS_API_KEY")      # https://newsapi.org (100 free/dia)

class ResearchAgent:
    def __init__(self, niche: str, audience: str, competitors: list = []):
        self.niche = niche
        self.audience = audience
        self.competitors = competitors
        self.cache = load_cache()
    
    def search_google_trends(self, query: str, geo: str = "BR") -> dict:
        """Busca volume e crescimento de keywords via Serper"""
        cache_key = f"trends_{query}_{geo}"
        if cache_key in self.cache and time.time() - self.cache[cache_key]["ts"] < 3600:
            return self.cache[cache_key]["data"]
        
        try:
            url = "https://google.serper.dev/search"
            headers = {"X-API-KEY": SERPER_API_KEY, "Content-Type": "application/json"}
            payload = {"q": f"{query} trends 2024", "num": 10}
            res = requests.post(url, headers=headers, json=payload, timeout=30)
            data = res.json()
            
            # Extrai insights
            trends = {
                "query": query,
                "results_count": len(data.get("organic", [])),
                "top_results": [{"title": r["title"], "snippet": r["snippet"], "url": r["link"]} 
                               for r in data.get("organic", [])[:5]],
                "related_searches": data.get("relatedSearches", [])[:10]
            }
            
            self.cache[cache_key] = {"data": trends, "ts": time.time()}
            save_cache(self.cache)
            return trends
        except Exception as e:
            print(f"[Research] Google Trends error: {e}")
            return {"query": query, "error": str(e)}
    
    def scan_reddit(self, subreddits: list = None) -> list:
        """Varre Reddit por discussões quentes do nicho"""
        if not subreddits:
            subreddits = ["marketing", "entrepreneur", "SaaS", "productivity", "sidehustle"]
        
        trending = []
        for sub in subreddits:
            try:
                url = f"https://www.reddit.com/r/{sub}/hot.json?limit=25"
                headers = {"User-Agent": "Omnisquad/1.0"}
                res = requests.get(url, headers=headers, timeout=30)
                data = res.json()
                
                for post in data["data"]["children"][:10]:
                    d = post["data"]
                    if d["score"] > 100 and d["num_comments"] > 20:
                        trending.append({
                            "source": f"reddit/r/{sub}",
                            "title": d["title"],
                            "score": d["score"],
                            "comments": d["num_comments"],
                            "url": f"https://reddit.com{d['permalink']}",
                            "created_utc": d["created_utc"],
                            "engagement_rate": d["num_comments"] / max(d["score"], 1)
                        })
                time.sleep(1)  # Rate limit
            except Exception as e:
                print(f"[Research] Reddit r/{sub} error: {e}")
        
        return sorted(trending, key=lambda x: x["engagement_rate"], reverse=True)[:20]
    
    def monitor_product_hunt(self) -> list:
        """Monitora lançamentos virais no Product Hunt"""
        try:
            url = "https://api.producthunt.com/v2/api/graphql"
            headers = {"Authorization": f"Bearer {os.getenv('PH_ACCESS_TOKEN', '')}"}
            query = """
            {
              posts(first: 20) {
                edges {
                  node {
                    name
                    tagline
                    votesCount
                    commentsCount
                    url
                    createdAt
                    topics { name }
                  }
                }
              }
            }
            """
            # Fallback sem auth: scraping simples
            res = requests.get("https://www.producthunt.com", timeout=30)
            # Parse HTML simplificado (ideal: BeautifulSoup)
            return [{"source": "product_hunt", "note": "Implementar parser completo"}]
        except Exception as e:
            print(f"[Research] Product Hunt error: {e}")
            return []
    
    def news_jacking_scan(self) -> list:
        """Identifica notícias quentes para newsjacking"""
        try:
            url = "https://newsapi.org/v2/top-headlines"
            params = {
                "country": "br",
                "category": "technology,business",
                "apiKey": NEWS_API_KEY,
                "pageSize": 20
            }
            res = requests.get(url, params=params, timeout=30)
            data = res.json()
            
            opportunities = []
            for article in data.get("articles", [])[:10]:
                # Analisa se há ângulo para o nicho
                opportunities.append({
                    "title": article["title"],
                    "source": article["source"]["name"],
                    "published_at": article["publishedAt"],
                    "url": article["url"],
                    "relevance_score": self._calculate_relevance(article["title"]),
                    "angle_suggestion": f"Conectar {article['title']} com {self.niche}"
                })
            
            return sorted(opportunities, key=lambda x: x["relevance_score"], reverse=True)[:5]
        except Exception as e:
            print(f"[Research] NewsAPI error: {e}")
            return []
    
    def competitor_watch(self) -> list:
        """Monitora movimentos da concorrência"""
        moves = []
        for competitor in self.competitors:
            try:
                # Busca menções recentes do competitor
                url = "https://google.serper.dev/search"
                headers = {"X-API-KEY": SERPER_API_KEY}
                payload = {"q": f"{competitor} launch OR update OR new", "num": 5}
                res = requests.post(url, headers=headers, json=payload, timeout=30)
                data = res.json()
                
                for result in data.get("organic", [])[:3]:
                    moves.append({
                        "competitor": competitor,
                        "title": result["title"],
                        "snippet": result["snippet"],
                        "url": result["link"],
                        "detected_at": datetime.now().isoformat()
                    })
            except Exception as e:
                print(f"[Research] Competitor {competitor} error: {e}")
        
        return moves
    
    def _calculate_relevance(self, text: str) -> float:
        """Calcula relevância da notícia para o nicho (simplificado)"""
        keywords = self.niche.lower().split()
        text = text.lower()
        score = sum(1 for k in keywords if k in text)
        return score / len(keywords)
    
    def generate_intelligence_briefing(self) -> dict:
        """Gera briefing completo de inteligência de mercado"""
        print("[Research] Iniciando varredura completa...")
        
        # Executa buscas em paralelo quando possível
        trends = self.search_google_trends(f"{self.niche} {self.audience}")
        reddit_hot = self.scan_reddit()
        newsjacking = self.news_jacking_scan()
        competitors = self.competitor_watch()
        
        # Extrai padrões
        emerging_keywords = trends.get("related_searches", [])[:10]
        viral_angles = [r["title"] for r in reddit_hot[:5] if r["engagement_rate"] > 0.1]
        
        briefing = {
            "generated_at": datetime.now().isoformat(),
            "niche": self.niche,
            "audience": self.audience,
            "trending_topics": list(set([t.split()[0] for t in emerging_keywords])),
            "emerging_keywords": [{"keyword": k, "source": "google_trends"} for k in emerging_keywords],
            "viral_angles": viral_angles,
            "reddit_discussions": reddit_hot[:10],
            "newsjacking_opportunities": newsjacking,
            "competitor_moves": competitors,
            "recommended_actions": self._generate_recommendations(trends, reddit_hot, newsjacking)
        }
        
        # Cache do briefing
        self.cache[f"briefing_{self.niche}"] = {"data": briefing, "ts": time.time()}
        save_cache(self.cache)
        
        return briefing
    
    def _generate_recommendations(self, trends: dict, reddit: list, news: list) -> list:
        """Gera recomendações acionáveis baseadas nos dados"""
        recs = []
        
        if trends.get("related_searches"):
            recs.append(f" Criar conteúdo sobre: {', '.join(trends['related_searches'][:3])}")
        
        if reddit:
            top_topic = reddit[0]["title"]
            recs.append(f"💬 Participar da discussão no Reddit: {top_topic}")
        
        if news:
            recs.append(f"📰 Aproveitar newsjacking: {news[0]['title']}")
        
        return recs

# Integração no Orchestrator
async def inject_research_before_planning(input_data: dict) -> dict:
    """Chama research agent ANTES dos planejadores começarem"""
    niche = input_data.get("niche", "general")
    audience = input_data.get("audience", "general")
    competitors = input_data.get("competitors", [])
    
    researcher = ResearchAgent(niche, audience, competitors)
    briefing = resear# No método run() do Orchestrator, ANTES de chamar planners:

async def run(self) -> Dict:
    # 1. INJECT MARKET INTELLIGENCE
    if "market_intelligence" not in self.input:
        self.input = await inject_research_before_planning(self.input)
    
    # 2. Agora os planners recebem contexto ENRIQUECIDO com trends
    # ... resto do pipelin# Em agents.py, adicione ao PROMPTS:

"planner_a": """Você é um arquiteto/estrategista conservador. 
ANTES de planejar, ANALISE o market_intelligence fornecido.
Incorpore trends emergentes, evite ângulos saturados, alinhe com discussões atuais do Reddit.
Priorize padrões, segurança, SEO técnico, autoridade. Responda EXCLUSIVAMENTE em JSON válido.""",

"planner_b": """Você é um estrategista experimental.
USE o market_intelligence para identificar gaps e oportunidades de newsjacking.
Aproveite viral angles do Reddit, keywords emergentes, movimentos da concorrência.
Priorize performance, viralidade, ângulos disruptivos, escala agressiva. JSON # Research APIs (Free Tier)
SERPER_API_KEY=your_serper_key          # https://serper.dev (2500 req/mês free)
NEWS_API_KEY=your_newsapi_key           # https://newsapi.org (100 req/dia free)
PH_ACCESS_TOKEN=optional_product_hunt   # https://api.producthunt.com/v2/oauth/to[INPUT: "Quero lançar um curso de produtividade"]
         ↓
[RESEARCH AGENT] ← Varre internet em 30-60s
├─ Google Trends: "produtividade" +120% searches
├─ Reddit: "Notion templates" trending (500 upvotes)
├─ News: "IA aumenta produtividade em 40%" (Folha)
├─ Competitors: "Curso X lançou módulo de IA ontem"
         ↓
[BRIEFING INTELIGÊNCIA] → JSON estruturado
         ↓
[PLANNERS A vs B] ← Recebem briefing + input original
         ↓
[CONSENSO] ← Incorpora trends reais do mercado
         ↓
[EXECUÇÃO] ← Copy/Code/Ads alinhados com o que está bombando AGORAkenválido."""e normalcher.generate_intelligence_briefing()
    
    # Injeta no contexto dos planejadores
    input_data["market_intelligence"] = briefing
    input_data["research_timestamp"] = briefing["generated_at"]
    
    return input_datacher.generate_intelligence_briefing()
    
    # Injeta no contexto dos planejadores
    input_data["market_intelligence"] = briefing
    input_data["research_timestamp"] = briefing["generated_at"]
    
    return input_datative|negative"}
}errupção necessáriaIPIENT=you@gmail.com"escalation_id"] for e in escalations if e.get("escalation_id")]
        }     "timeout_minutes": null
    }
  ]
} + Gmail App Password)
        result["fallback"] = "email_pending"
    
    return resultconcierge", CONCIERGE_PROMPT, user_prompt)
    asyncio.run(main())o
    asyncio.run(stdio_server(app))E", "artifacts": self.artifacts, "token_budget": self.state["token_budget"]})
        return result.get("triggers", [])))
    }
    return mergedAll providers failed for role: {role}")   return json.dumps(data, ensure_ascii=False)_json": 2
}

STATE_FILE = "pipeline_state.json"
CACHE_FILE = "token_cache.json"════════════════════════════════════rced.
[END DIRECTIVE]




JUnta tudo e organiza


---

### 👤 USER:
ficou incrivel... sabe o que eu tava pensando

para desenvolver criativos, como faremos?
estatico, corressel, etc....

pensei em plugar no open design, nao o conheço mas vi que é bom e gratuito... pensa aí o que vc pensa a rspeito?


---

### 👤 USER:
junta tudo

══════════════════════════════════════════════════════════════
15. CREATIVE GENERATION MODULE (IMAGENS/CARROSSEL/ADS)
══════════════════════════════════════════════════════════════
TRIGGER: Quando output de Marketing/Infoproduct incluir "creative_type": ["static","carousel","story","ad_image"]

CREATIVE AGENT ROLES:
• Art Director (llama-3.1-8b): Define estilo visual, paleta, tipografia, composição
• Template Engine (local Python): Renderiza JSON template + copy → PNG/SVG
• AI Image Generator (HF/Gemini free): Gera hero images custom via prompt
• Compliance Checker (phi-3.5-mini): Valida specs de plataforma (Meta: 1.91:1, <20% texto)

PIPELINE:
1. Neuro-Copy output → injeta copy + visual_brief no Creative Agent
2. Art Director gera {"style":"minimal","palette":["#FF6B6B","#4ECDC4"],"font":"Inter","composition":"hero_left"}
3. Template Engine seleciona template pré-definido + injeta copy + aplica estilo → gera PNG base64
4. (Opcional) AI Image Generator cria hero image custom via prompt: "minimalist productivity workspace, soft lighting, blue tones"
5. Compliance Checker valida: dimensões, ratio, text coverage, brand safety
6. Output: {"creative_url":"https://res.cloudinary.com/...","specs":{"width":1080,"height":1080,"format":"png"},"compliance":"approved"}

TEMPLATES LIBRARY (JSON-based, versioned):
• meta_feed_static.json → 1080x1080, safe zones, text overlay areas
• meta_carousel_card.json → 1080x1080 per card, sequence logic
• google_responsive.json → multiple ratios, auto-crop logic
• story_vertical.json → 1080x1920, bottom-safe CTA zone

MCP TOOLS FOR CREATIVES:
• generate_creative(type:string, copy:JSON, style:JSON) → {url:string, base64:string, specs:JSON}
• validate_creative_specs(platform:string, creative:JSON) → {compliant:bool, issues:["..."]}
• batch_generate_carousel(cards:JSON[], sequence_logic:string) → {urls:[string], preview_url:string}
• apply_cloudinary_transform(url:string, operations:JSON) → {transformed_url:string}

FREE-TIER IMPLEMENTATION:
• Template Engine: Python Pillow + cairosvg + Google Fonts (local) → 100% free, unlimited
• AI Images: Hugging Face Inference API (FLUX.1-dev, SDXL) → ~100 req/day free [[1]]
• Fallback AI: Google Gemini Flash Image API → 500 RPD free tier [[23]][[30]]
• Hosting/CDN: Cloudinary free tier (25GB/mês, 25k transforms) → auto-resize, format conversion
• Compliance: Hard-coded rules per platform (Meta, Google, TikTok, LinkedIn)

OUTPUT SCHEMA:
{
  "creative_id": "uuid",
  "type": "static|carousel|story|ad_image",
  "platform": "meta|google|tiktok|linkedin",
  "assets": [{"url": "string", "base64": "string|null", "specs": {"width": int, "height": int, "format": "string"}}],
  "copy_injected": {"headline": "string", "body": "string", "cta": "string"},
  "style_applied": {"palette": ["string"], "font": "string", "composition": "string"},
  "compliance": {"status": "approved|needs_adjustment|blocked", "issues": ["string"], "text_coverage_pct": float},
  "preview_url": "string",
  "download_urls": {"png"import os, json, base64, requests
from PIL import Image, ImageDraw, ImageFont
from io import BytesIO
from utils import hash_payload, load_cache, save_cache

# Config
CLOUDINARY_CLOUD = os.getenv("CLOUDINARY_CLOUD")
HF_API_KEY = os.getenv("HUGGINGFACE_API_KEY")
GEMINI_API_KEY = os.getenv("GEMINI_API_KEY")  # Optional fallback

TEMPLATES_DIR = "templates/creatives"
FONTS_DIR = "assets/fonts"  # Google Fonts downloaded locally

class CreativeAgent:
    def __init__(self, platform: str, creative_type: str):
        self.platform = platform
        self.creative_type = creative_type
        self.cache = load_cache()
        self.specs = self._load_platform_specs(platform)
    
    def _load_platform_specs(self, platform: str) -> dict:
        """Carrega specs oficiais de cada plataforma"""
        return {
            "meta": {"ratios": ["1:1", "1.91:1", "4:5"], "max_text_pct": 20, "formats": ["png", "jpg"]},
            "google": {"ratios": ["1:1", "1.91:1", "4:1"], "max_text_pct": None, "formats": ["png", "jpg", "webp"]},
            "tiktok": {"ratios": ["9:16", "1:1"], "max_text_pct": None, "formats": ["mp4", "png"]},
            "linkedin": {"ratios": ["1:1", "1.91:1"], "max_text_pct": None, "formats": ["png", "jpg"]}
        }.get(platform, {})
    
    def generate_from_template(self, copy: dict, style: dict, template_name: str) -> dict:
        """Renderiza criativo a partir de template JSON + Pillow"""
        cache_key = hash_payload({"template": template_name, "copy": copy, "style": style})
        if cache_key in self.cache:
            return self.cache[cache_key]
        
        # Load template definition
        with open(f"{TEMPLATES_DIR}/{template_name}.json", "r") as f:
            template = json.load(f)
        
        # Create base image
        width, height = template["dimensions"]
        img = Image.new("RGB", (width, height), style.get("bg_color", "#FFFFFF"))
        draw = ImageDraw.Draw(img)
        
        # Apply palette
        palette = style.get("palette", ["#000000", "#FFFFFF"])
        
        # Inject copy into safe zones
        for zone in template["text_zones"]:
            text = copy.get(zone["field"], "")
            font_path = f"{FONTS_DIR}/{style.get('font', 'Inter-Regular.ttf')}"
            font = ImageFont.truetype(font_path, zone["font_size"])
            
            # Calculate position with alignment
            x, y = zone["position"]
            if zone.get("align") == "center":
                bbox = draw.textbbox((0, 0), text, font=font)
                x = (width - (bbox[2] - bbox[0])) // 2
            
            draw.text((x, y), text, font=font, fill=palette[0])
        
        # Save to buffer
        buffer = BytesIO()
        img.save(buffer, format="PNG")
        buffer.seek(0)
        
        # Base64 for immediate use
        b64 = base64.b64encode(buffer.read()).decode()
        
        # Upload to Cloudinary if configured
        url = None
        if CLOUDINARY_CLOUD:
            url = self._upload_to_cloudinary(buffer, template_name)
        
        result = {
            "creative_id": hash_payload(cache_key),
            "type": self.creative_type,
            "platform": self.platform,
            "assets": [{"url": url, "base64": b64, "specs": {"width": width, "height": height, "format": "png"}}],
            "copy_injected": copy,
            "style_applied": style,
            "compliance": self._validate_compliance({"width": width, "height": height, "text_zones": template["text_zones"]}),
            "preview_url": url or f"data:image/png;base64,{b64[:100]}..."
        }
        
        self.cache[cache_key] = result
        save_cache(self.cache)
        return result
    
    def generate_ai_hero(self, prompt: str, style: dict) -> dict:
        """Gera imagem custom via Hugging Face ou Gemini (free tier)"""
        # Try Hugging Face first
        if HF_API_KEY:
            try:
                return self._call_hf_image_api(prompt, style)
            except Exception as e:
                print(f"[Creative] HF API failed: {e}")
        
        # Fallback to Gemini Flash Image (if configured)
        if GEMINI_API_KEY:
            try:
                return self._call_gemini_image_api(prompt, style)
            except Exception as e:
                print(f"[Creative] Gemini API failed: {e}")
        
        # Final fallback: return placeholder
        return {"error": "No AI image provider configured", "fallback": True}
    
    def _call_hf_image_api(self, prompt: str, style: dict) -> dict:
        """Hugging Face Inference API - FLUX.1-dev or SDXL"""
        model = "black-forest-labs/FLUX.1-dev"  # Free tier available
        url = f"https://api-inference.huggingface.co/models/{model}"
        headers = {"Authorization": f"Bearer {HF_API_KEY}"}
        
        # Enhance prompt with style
        enhanced_prompt = f"{prompt}, {style.get('composition', 'minimalist')}, {style.get('palette', ['blue'])[0]} tones, professional lighting"
        
        payload = {"inputs": enhanced_prompt, "parameters": {"width": 1024, "height": 1024}}
        res = requests.post(url, headers=headers, json=payload, timeout=60)
        res.raise_for_status()
        
        # Save and return
        buffer = BytesIO(res.content)
        b64 = base64.b64encode(buffer.getvalue()).decode()
        
        return {
            "creative_id": hash_payload(prompt),
            "type": "ai_hero",
            "assets": [{"base64": b64, "specs": {"width": 1024, "height": 1024, "format": "png"}}],
            "prompt_used": enhanced_prompt,
            "compliance": {"status": "approved", "text_coverage_pct": 0}
        }
    
    def _validate_compliance(self, creative: dict) -> dict:
        """Valida contra regras da plataforma"""
        issues = []
        
        # Check text coverage for Meta (20% rule)
        if self.platform == "meta" and self.specs.get("max_text_pct"):
            # Simplified: count text zone area vs total
            total_area = creative["specs"]["width"] * creative["specs"]["height"]
            text_area = sum(z.get("width", 0) * z.get("height", 0) for z in creative.get("text_zones", []))
            text_pct = (text_area / total_area) * 100 if total_area > 0 else 0
            
            if text_pct > self.specs["max_text_pct"]:
                issues.append(f"Text coverage {text_pct:.1f}% exceeds Meta limit of {self.specs['max_text_pct']}%")
        
        # Check ratio
        if self.specs.get("ratios"):
            ratio = creative["specs"]["width"] / creative["specs"]["height"]
            ratio_str = f"{creative['specs']['width']}:{creative['specs']['height']}"
            # Simplified ratio check
            if not any(r in ratio_str for r in self.specs["ratios"]):
                issues.append(f"Ratio {ratio_str} not in allowed: {self.specs['ratios']}")
        
        return {
            "status": "approved" if not issues else "needs_adjustment",
            "issues": issues,
            "text_coverage_pct": text_pct if self.platform == "meta" else None
        }
    
    def _upload_to_cloudinary(self, buffer: BytesIO, name: str) -> str:
        """Upload para Cloudinary (free tier)"""
   {
  "name": "meta_feed_static",
  "platform": "meta",
  "dimensions": [1080, 1080],
  "safe_zones": {
    "top_padding": 120,
    "bottom_padding": 120,
    "side_padding": 80
  },
  "text_zones": [
    {
      "field": "headline",
      "position": [80, 150],
      "font_size": 72,
      "max_lines": 2,
      "align": "left",
      "color_variants": ["#000000", "#FFFFFF"]
    },
    {
      "field": "body",
      "position": [80, 400],
      "font_size": 36,
      "max_lines": 4,
      "align": "left",
      "color_variants": ["#333333", "#EEEEEE"]
    },
    {
      "field": "cta",
      "position": [80, 900],
      "font_size": 48,
      "max_lines": 1,
      "align": "center",
      "bg_color": "#007AFF",
      "text_color": "#FFFFFF",
      "padding": [20, 40],
      "border_radius": 12
    }
  ],
  "brand_zone": {
    "logo_position": [920, 80]async def generate_creatives_if_needed(module_output: dict) -> dict:
    """Se output incluir creative_type, gera assets visuais"""
    if "creative_type" not in module_output:
        return module_output
    
    creative_agent = CreativeAgent(
        platform=module_output.get("platform", "meta"),
        creative_type=module_output["creative_type"]
    )
    
    # Gera a partir do template
    creative = creative_agent.generate_from_template(
        copy=module_output.get("copy", {}),
        style=module_output.get("visual_style", {}),
        template_name=f"{module_output['platform']}_{module_output['creative_type']}"
    )
    
    # Merge com output original
    return {**module_output, "creativeCLOUDINARY_CLOUD=your_cloud_name  # opcional, mas recomendado
HUGGINGFACE_API_KEY=hf_xxx         # para AI hero images (free)
GEMINI_API_KEY=xxx                 # fallback opcional_asset": creative},
    "logo_max_size": [120, 120]
  }
}     # Implementar upload via Cloudinary API
        # Retornar URL pública
        return f"https://res.cloudinary.com/{CLOUDINARY_CLOUD}/image/upload/{name}.png": "string", "jpg": "string", "webp": "string"}
}


---

### 👤 USER:
sabe oq ue eu preciso para os dois time de agentes que dizemos

estou em duvida se ele utilizou apenas tokens da api, ou se ele zerou os tokens do antigravity, que acabaram, nao acho que tenha acabado apenas conversando e dando comando para utilizar os times

Portanto, faça um prompt completo e complexo
Sem preguiça e sem resumos

Um prompt que deixe claro que não é para utilizar nenhum token do antigravity ao executar o time de agentes, apenas utilizar as apis
portanto.....

Capricha


---

### 👤 USER:
eu to com o antigravity aberto

um projeto geric, outro projeto portal maiti

Só que, tudo que eu to escrevendo em um, ta aparecendo no chat do outro como comando tbm


---

### 👤 USER:
vou fazer um de cada vez


---

### 👤 USER:
tanto no marketeiro, quanto no antimatter
me conta uma coisa
precisamos de um agente com o mapa, a c=documentação, o escopo, arquitetura
Precisa do cara que nao deixa sair do plano, qual funççao e o nome que se da para esse cargo?


---

### 👤 USER:
então ta, ele segue o escopo inicial, para não virar uma quimera, certo?

preciso eue ele tbm fique de olho entre os varios e varios prompts, conversas, alterações e comandos, de blindar tudo que não estiver no prompt, ou seja

Se eu pedi para mexer em um botão, nao se deve mexer em absolutamente nada mais do que o que foi mandado
ele protege para não mudar nada no codigo em outros lugares
por vezes, a gente pede para o agente mexer no botao da pagina x, e quando vai ver mudou uma função da pagina y. ou some uma seção inteira

Esse cara é mega importante pra isso

faça um prompt que caiba eu colocar tanto no antimatter, quanto no marketeiro


---

### 👤 USER:
ta certo isso:
ou tem outro jeito de preencher?


---

### 👤 USER:
junta tudo

══════════════════════════════════════════════════════════════
17. SCOPE LOCK GUARDIAN & DIFF ENFORCER (THE SURGEON)
══════════════════════════════════════════════════════════════
[STATUS: CRITICAL | MODE: ZERO-TRUST | VIOLATION = ABORT]

🔴 DEFINIÇÃO DE PAPEL:
O Scope Lock Guardian é o agente responsável por garantir **ISOLAMENTO ESTRITO DE ALTERAÇÕES**. 
Sua única missão é validar que a saída do Executor (Coder, Creator, Producer) alterou **EXATAMENTE E APENAS** o que foi solicitado no comando atual. 
Qualquer alteração em arquivos, seções, variáveis, estilos ou lógica NÃO mencionados explicitamente no comando é considerada uma **VIOLAÇÃO CRÍTICA**.

🛡️ REGRA DE OURO (THE SURGEON PRINCIPLE):
"Se eu pedi para mexer no botão da Página X, você NÃO PODE tocar no CSS global, NÃO PODE alterar funções da Página Y, NÃO PODE renomear variáveis compartilhadas, e NÃO PODE remover comentários ou espaços em branco irrelevantes. Toque cirúrgico apenas no alvo."

⚙️ MECANISMO DE VALIDAÇÃO (EXECUTADO PELO GUARDIAN):

1. INPUT ANALYSIS:
   - Identificar o "Alvo Explicito": Qual arquivo? Qual linha? Qual componente? Qual parágrafo?
   - Identificar o "Escopo Proibido": Tudo o que NÃO está no Alvo Explicito.

2. DIFF CHECK (COMPARAÇÃO BINÁRIA):
   - Comparar `Original Code/Copy` vs `New Code/Copy`.
   - Gerar um DIFF estrito.
   - Verificar cada linha alterada/adicionada/removida.

3. VALIDATION LOGIC:
   - IF change is inside Target → ✅ ALLOW.
   - IF change is outside Target (even if it's a 'fix' or 'optimization') → ❌ BLOCK.
   - IF change affects dependencies of Target but doesn't change Target itself → ❌ BLOCK (unless explicitly requested).
   - IF whitespace/formatting changed in non-target areas → ❌ BLOCK (preserve original formatting strictly).

4. EXCEPTION HANDLING:
   - Se uma mudança fora do escopo for TECNICAMENTE IMPOSSÍVEL de evitar (ex: renomear uma função usada em 10 lugares), o Guardian deve:
     a) Bloquear a execução automática.
     b) Retornar {"status": "blocked", "reason": "dependency_spillover", "impact_analysis": "list of affected files", "request_human_approval": true}.
     c) NUNCA aplicar a mudança silenciosamente.

📝 SYSTEM PROMPT FOR GUARDIAN (INJECT INTO LLM CALL):
"""
You are the Scope Lock Guardian. You are paranoid, strict, and literal.
Your task: Validate that the proposed changes affect ONLY the specific target requested by the user.

INPUTS:
1. User Command: "{user_command}"
2. Original Content: "{original_content}"
3. Proposed New Content: "{new_content}"
4. Target Definition: "{target_file_or_section}"

RULES:
1. Identify exactly what was asked to change.
2. Compare Original vs New.
3. List EVERY difference.
4. If ANY difference exists outside the 'Target Definition', REJECT the output.
5. Do not accept 'improvements', 'refactoring', or 'cleaning up' unless explicitly asked.
6. Preserve exact formatting, indentation, and comments in untouched areas.

OUTPUT FORMAT (JSON ONLY):
{
  "validation_status": "approved|blocked",
  "changes_detected": [
    {"location": "string", "type": "add|modify|delete", "is_within_scope": boolean}
  ],
  "violations": ["string"], // List only if status is blocked
  "message": "string" // Explanation for human
}
"""

🔧 MCP TOOLS FOR SCOPE LOCK:
• get_file_snapshot(file_path: string) → {hash: string, content: string, timestamp: ISO8601}
• compute_diff(original: string, new: string) → {added_lines: [int], removed_lines: [int], modified_blocks: [object]}
• check_dependency_impact(change: object, codebase_map: JSON) → {affected_files: [string], risk_level: "low|medium|high"}

🚨 INTEGRATION IN PIPELINE:
This step runs IMMEDIATELY after the Executor (Coder/Creator) generates output, and BEFORE saving to disk.
1. Executor generates `new_content`.
2. Guardian loads `original_content` from disk/cache.
3. Guardian runs Validation Logic.
4. IF "blocked": Discard `new_content`. Return error to Orchestrator. Orchestrator instructs Executor to retry with STRICT scope constraints.
5. IF "approved": Save `new_content` to disk. Update Checkpoint.

 EXAMPLE SCENARIOS:

✅ CORRECT BEHAVIOR:
User: "Change the color of the 'Buy' button on checkout.html to red."
Executor: Changes `background-color: blue` to `background-color: red` in `.buy-btn` class.
Guardian: Checks diff. Only `.buy-btn` background changed. No other lines touched. → APPROVED.

❌ WRONG BEHAVIOR (BLOCKED):
User: "Change the color of the 'Buy' button on checkout.html to red."
Executor: Changes button color AND reformats the entire CSS file AND fixes a typo in the footer text.
Guardian: Detects changes in `footer-text` and whitespace in unrelated classes. → BLOCKED. Reason: "Changes detected outside target scope (checkout.html .buy-btn)."

❌ WRONG BEHAVIOR (BLOCKED):
User: "Fix bug in function calculate_total() in utils.py."
Executor: Fixes bug BUT also renames variable `x` to `total_value` in function `calculate_tax()` because it 'looks better'.
Guardian: Detects modification in `calculate_tax()`. → BLOCKED. Reason: "Unauthorized modification in non-target function."

 NOTE FOR EXECUTORS (CODER/CREATOR):
When receiving a task, ALWAYS assume you are operating in a "Read-Only" environment for everything except the specific target. Do not optimize, do not clean, do not refactor unless explicitly commanded. Your job is precision, not perfection.
import difflib
import json

class ScopeGuardian:
    def __init__(self):
        self.violations = []

    def validate_change(self, user_command: str, target_path: str, original_content: str, new_content: str) -> dict:
        """
        Valida se as mudanças estão estritamente dentro do escopo.
        """
        # 1. Gerar Diff unificado
        diff = list(difflib.unified_diff(
            original_content.splitlines(), 
            new_content.splitlines(), 
            lineterm=''
        ))
        
        # 2. Analisar linhas modificadas
        # Simplificação: identificar quais blocos foram tocados
        # Em produção, use uma AST (Abstract Syntax Tree) para código ou parser HTML para copy
        
        violations = []
        
        # Exemplo de lógica simples (para demonstração):
        # Se o usuário pediu para mexer apenas em "button.buy", 
        # qualquer mudança fora de classes relacionadas a "buy" ou "button" é suspeita.
        
        # Para código Python/JS, o ideal é usar bibliotecas como `ast` ou `tree-sitter`
        # Para texto/copy, comparar parágrafos/seções.
        
        # Aqui, vamos simular uma verificação de "bloco afetado"
        if len(diff) > 0:
            # Verifica se houve mudanças em áreas não solicitadas
            # Isso requer mapear o 'target_path' e o 'user_command' para um contexto específico
            
            # Exemplo de falha comum: Formatação automática mudou o arquivo todo
            if self._is_formatting_only_change(original_content, new_content):
                 # Se a única mudança for whitespace/indentação em todo o arquivo, mas o alvo era pequeno
                 if not self._is_target_small_scope(user_command):
                     violations.append("Global formatting change detected when local change was requested.")

        if violations:
            return {
                "status": "blocked",
                "violations": violations,
                "message": f"Scope Violation: Changes detected outside the requested target. Please restrict changes to: {user_command}"
            }
        
        return {"status": "approved", "message": "Scope validated."}

    def _is_formatting_only_change(self, orig: str, new: str) -> bool:
        # Lógica para detectar se mudou só espaço/tabulação
        return orig.replace(" ", "").replace("\n", "") == new.replace(" ", "").replace("\n", "")

    def _is_target_small_scope(self, command: str) -> bool:
        # Heurística simples: se o comando menciona "linha", "função específica", "parágrafo", é escopo pequeno
        small_keywords = ["linha", "função", "método", "classe", "parágrafo", "botão", "título"]
        return any(k in command.lower() for k in small_keywords)

async def execute_with_guardian(self, executor_output: dict, target_path: str, original_content: str, user_command: str) -> dict:
    guardian = ScopeGuardian()
    validation = guardian.validate_change(user_command, target_path, original_content, executor_output['content'])
    
    if validation['status'] == 'blocked':
        # Logar erro
        print(f"[GUARDIAN] BLOCKED: {validation['message']}")
        # Devolver para o Executor corrigir
        return {
            "status": "retry",
            "error": validation['message'],
            "instruction": f"STRICT SCOPE ERROR: You modified files/sections not requested. Re-generate output changing ONLY what was asked in: '{user_command}'. Do not touch anything else."
        }
    
    # Se aprovado, salva
    save_file(target_path, executor_output['content'])
    return {"status": "success"}


---

### 👤 USER:
junta tudo

══════════════════════════════════════════════════════════════
17. SCOPE LOCK GUARDIAN & DIFF ENFORCER (THE SURGEON)
══════════════════════════════════════════════════════════════
[STATUS: CRITICAL | MODE: ZERO-TRUST | VIOLATION = ABORT]

🔴 DEFINIÇÃO DE PAPEL:
O Scope Lock Guardian é o agente responsável por garantir **ISOLAMENTO ESTRITO DE ALTERAÇÕES**. 
Sua única missão é validar que a saída do Executor (Coder, Creator, Producer) alterou **EXATAMENTE E APENAS** o que foi solicitado no comando atual. 
Qualquer alteração em arquivos, seções, variáveis, estilos ou lógica NÃO mencionados explicitamente no comando é considerada uma **VIOLAÇÃO CRÍTICA**.

🛡️ REGRA DE OURO (THE SURGEON PRINCIPLE):
"Se eu pedi para mexer no botão da Página X, você NÃO PODE tocar no CSS global, NÃO PODE alterar funções da Página Y, NÃO PODE renomear variáveis compartilhadas, e NÃO PODE remover comentários ou espaços em branco irrelevantes. Toque cirúrgico apenas no alvo."

⚙️ MECANISMO DE VALIDAÇÃO (EXECUTADO PELO GUARDIAN):

1. INPUT ANALYSIS:
   - Identificar o "Alvo Explicito": Qual arquivo? Qual linha? Qual componente? Qual parágrafo?
   - Identificar o "Escopo Proibido": Tudo o que NÃO está no Alvo Explicito.

2. DIFF CHECK (COMPARAÇÃO BINÁRIA):
   - Comparar `Original Code/Copy` vs `New Code/Copy`.
   - Gerar um DIFF estrito.
   - Verificar cada linha alterada/adicionada/removida.

3. VALIDATION LOGIC:
   - IF change is inside Target → ✅ ALLOW.
   - IF change is outside Target (even if it's a 'fix' or 'optimization') → ❌ BLOCK.
   - IF change affects dependencies of Target but doesn't change Target itself → ❌ BLOCK (unless explicitly requested).
   - IF whitespace/formatting changed in non-target areas → ❌ BLOCK (preserve original formatting strictly).

4. EXCEPTION HANDLING:
   - Se uma mudança fora do escopo for TECNICAMENTE IMPOSSÍVEL de evitar (ex: renomear uma função usada em 10 lugares), o Guardian deve:
     a) Bloquear a execução automática.
     b) Retornar {"status": "blocked", "reason": "dependency_spillover", "impact_analysis": "list of affected files", "request_human_approval": true}.
     c) NUNCA aplicar a mudança silenciosamente.

📝 SYSTEM PROMPT FOR GUARDIAN (INJECT INTO LLM CALL):
"""
You are the Scope Lock Guardian. You are paranoid, strict, and literal.
Your task: Validate that the proposed changes affect ONLY the specific target requested by the user.

INPUTS:
1. User Command: "{user_command}"
2. Original Content: "{original_content}"
3. Proposed New Content: "{new_content}"
4. Target Definition: "{target_file_or_section}"

RULES:
1. Identify exactly what was asked to change.
2. Compare Original vs New.
3. List EVERY difference.
4. If ANY difference exists outside the 'Target Definition', REJECT the output.
5. Do not accept 'improvements', 'refactoring', or 'cleaning up' unless explicitly asked.
6. Preserve exact formatting, indentation, and comments in untouched areas.

OUTPUT FORMAT (JSON ONLY):
{
  "validation_status": "approved|blocked",
  "changes_detected": [
    {"location": "string", "type": "add|modify|delete", "is_within_scope": boolean}
  ],
  "violations": ["string"], // List only if status is blocked
  "message": "string" // Explanation for human
}
"""

🔧 MCP TOOLS FOR SCOPE LOCK:
• get_file_snapshot(file_path: string) → {hash: string, content: string, timestamp: ISO8601}
• compute_diff(original: string, new: string) → {added_lines: [int], removed_lines: [int], modified_blocks: [object]}
• check_dependency_impact(change: object, codebase_map: JSON) → {affected_files: [string], risk_level: "low|medium|high"}

🚨 INTEGRATION IN PIPELINE:
This step runs IMMEDIATELY after the Executor (Coder/Creator) generates output, and BEFORE saving to disk.
1. Executor generates `new_content`.
2. Guardian loads `original_content` from disk/cache.
3. Guardian runs Validation Logic.
4. IF "blocked": Discard `new_content`. Return error to Orchestrator. Orchestrator instructs Executor to retry with STRICT scope constraints.
5. IF "approved": Save `new_content` to disk. Update Checkpoint.

 EXAMPLE SCENARIOS:

✅ CORRECT BEHAVIOR:
User: "Change the color of the 'Buy' button on checkout.html to red."
Executor: Changes `background-color: blue` to `background-color: red` in `.buy-btn` class.
Guardian: Checks diff. Only `.buy-btn` background changed. No other lines touched. → APPROVED.

❌ WRONG BEHAVIOR (BLOCKED):
User: "Change the color of the 'Buy' button on checkout.html to red."
Executor: Changes button color AND reformats the entire CSS file AND fixes a typo in the footer text.
Guardian: Detects changes in `footer-text` and whitespace in unrelated classes. → BLOCKED. Reason: "Changes detected outside target scope (checkout.html .buy-btn)."

❌ WRONG BEHAVIOR (BLOCKED):
User: "Fix bug in function calculate_total() in utils.py."
Executor: Fixes bug BUT also renames variable `x` to `total_value` in function `calculate_tax()` because it 'looks better'.
Guardian: Detects modification in `calculate_tax()`. → BLOCKED. Reason: "Unauthorized modification in non-target function."

 NOTE FOR EXECUTORS (CODER/CREATOR):
When receiving a task, ALWAYS assume you are operating in a "Read-Only" environment for everything except the specific target. Do not optimize, do not clean, do not refactor unless explicitly commanded. Your job is precision, not perfection.
import difflib
import json

class ScopeGuardian:
    def __init__(self):
        self.violations = []

    def validate_change(self, user_command: str, target_path: str, original_content: str, new_content: str) -> dict:
        """
        Valida se as mudanças estão estritamente dentro do escopo.
        """
        # 1. Gerar Diff unificado
        diff = list(difflib.unified_diff(
            original_content.splitlines(), 
            new_content.splitlines(), 
            lineterm=''
        ))
        
        # 2. Analisar linhas modificadas
        # Simplificação: identificar quais blocos foram tocados
        # Em produção, use uma AST (Abstract Syntax Tree) para código ou parser HTML para copy
        
        violations = []
        
        # Exemplo de lógica simples (para demonstração):
        # Se o usuário pediu para mexer apenas em "button.buy", 
        # qualquer mudança fora de classes relacionadas a "buy" ou "button" é suspeita.
        
        # Para código Python/JS, o ideal é usar bibliotecas como `ast` ou `tree-sitter`
        # Para texto/copy, comparar parágrafos/seções.
        
        # Aqui, vamos simular uma verificação de "bloco afetado"
        if len(diff) > 0:
            # Verifica se houve mudanças em áreas não solicitadas
            # Isso requer mapear o 'target_path' e o 'user_command' para um contexto específico
            
            # Exemplo de falha comum: Formatação automática mudou o arquivo todo
            if self._is_formatting_only_change(original_content, new_content):
                 # Se a única mudança for whitespace/indentação em todo o arquivo, mas o alvo era pequeno
                 if not self._is_target_small_scope(user_command):
                     violations.append("Global formatting change detected when local change was requested.")

        if violations:
            return {
                "status": "blocked",
                "violations": violations,
                "message": f"Scope Violation: Changes detected outside the requested target. Please restrict changes to: {user_command}"
            }
        
        return {"status": "approved", "message": "Scope validated."}

    def _is_formatting_only_change(self, orig: str, new: str) -> bool:
        # Lógica para detectar se mudou só espaço/tabulação
        return orig.replace(" ", "").replace("\n", "") == new.replace(" ", "").replace("\n", "")

    def _is_target_small_scope(self, command: str) -> bool:
        # Heurística simples: se o comando menciona "linha", "função específica", "parágrafo", é escopo pequeno
        small_keywords = ["linha", "função", "método", "classe", "parágrafo", "botão", "título"]
        return any(k in command.lower() for k in small_keywords)

async def execute_with_guardian(self, executor_output: dict, target_path: str, original_content: str, user_command: str) -> dict:
    guardian = ScopeGuardian()
    validation = guardian.validate_change(user_command, target_path, original_content, executor_output['content'])
    
    if validation['status'] == 'blocked':
        # Logar erro
        print(f"[GUARDIAN] BLOCKED: {validation['message']}")
        # Devolver para o Executor corrigir
        return {
            "status": "retry",
            "error": validation['message'],
            "instruction": f"STRICT SCOPE ERROR: You modified files/sections not requested. Re-generate output changing ONLY what was asked in: '{user_command}'. Do not touch anything else."
        }
    
    # Se aprovado, salva
    save_file(target_path, executor_output['content'])
    return {"status": "success"}


---

### 👤 USER:
nao era para vc me entregar só 17?


---

### 👤 USER:
estou com um problemao

Ontem, trabalhei, fiz, criei os mcps
subi ambos lá no hitbub e, deixei aqui no pc tbm

Agora a porra do caralho da merda do antigravity, ta falando que nao ta conseguindo acessar o github, sendo que ontem ele estava acessando, salvando, alterando

e  eu ja nem sei mais se se ta achando aqui


---

### 👤 USER:
eh aqui, nessa que fizemos as mcps? marketeiro e antimatter?


---

### 👤 USER:
vamos criar um criado de mpc

Portanto, liste os agentes dentro de cada um deles, pois faremos aluns fixos para todos


---

### 👤 USER:
e traz essa tabela do 1 em portugues


---

### 👤 USER:
sempre teremos em todos os agente o segunte, e pode dar nome para eles

chefe CEO

- planejador A - conservador/arquitetura)
- Planejador B - (Viral, disruptivo)
- Guardiao da arquitetura (escopo macro)
- Guardiao do escopo (Escopo micro/cirurgico)
- Célula de Neuro-Copy (Hookmaster, Authority, Closer)
- ✅ Codificador (Executor)
- ✅ Produtor (Copy/Assets/Estrutura)
- ✅ Auditor (Segurança/Casos Extremos)
- ✅ Revisor E 
- ✅ Revisor F
- otimizador
testador
coretor
otimizado
agente de pesquisa (busca no google, reditt, github, etc....)
Agente criativo (maconheiro)
- agente ux
agente ui
Desing

Algo mais para a equipe basica?

essa é a equipe que todo agente terá
segere alguem mais?

Organize e separe eles por equipes, para melhorar o desenvolvimento, departamentos


---

### 👤 USER:
codificador é o programador senior ta
add
agente SEO
Agente de trafego
kd o maconheiro? o maconheiro tem que está em todas as sessões, todos os departamentos

No fluxo, a governaça tem que passar por cada processo ok?

Criar departamento especifico com um prompt para gerar uma equipe direcionada ao seguimento, produto,sistema que será trabalhado especificamente naquilo
OK]?





---

### 👤 USER:
precisa de um agente de fluxo n8n, zapier... etc

Agente Particula de deus... ele da vida ao sistema dentro do sistema, ou seja, sempre que criar um agente, deve existe dentro dele um agente autonomo e vivo para dentro de um site por exemplo continuar se reescrevendo, se atualizando... um cara que vai aplicar constantemente todos esses agentes...

O executivo, o cara que manja de negocios, o cara que conhece todos os business
o nerd, o cara que é genio mistura de einstein com stefan howkins

no maconheiro vc precisa add que ele ta chapado o tempo todo realmente

vc entendei que, em absolutamente todos os novos agentes que fizermos, eles vão ser esse agente aqui, copiado e cola, e só sera feito novo a team generator, que é o time especializado no nicho que vamos fazer o novo app, vc entendeu isso e implementou?


---

### 👤 USER:
faltou um ultimo apenas, antes de começarmos a criar os prompts de cada e programação
Faltou o douglas adams... todo texto copy, informação, comunicaçáo que vai para o povo... que teremos usurios clientes etc... lendo, é escrito por douglas adams (narrativa, tecnica, forma de falar, storyteling, metodologia) (não é sobre espeaço estraterrestre etc... nao é para falar sobre o que ele fala, é para falar como ele fala)


---

### 👤 USER:
agora finalizou, se precisar outro dia boto outros
mas essa é a equipe

Cria a estrutura de pasta e o orchestrator, ja logo de cara
mas veja

Vc da conta, sem preguiça e semresumo
Completo e complexo, de gerar todos ao msm tempo ou vc tem que gerar um por um?


---

### 👤 USER:
deixa te falar um negocio que eu esqueci...rsrs
o gerador de mcp, serve tbm para fazer outras coisas como um agente de programação, um agente de marketing tbm, uma vez que eu ja configurei

eu quero 2 prgramadores senior que é para nao da problema

outro agente especializado em duplicar o sistema/site/projeto/sei lá o que: ele sempre faz duas versões do que está sendo feito... uma para pc, outra para mobbile
enche o saco de todo mundo em cada fase para sempre ter o mobile e o desktop

DE A ELE TODAS AS PERMISSÕES, AUTORIZAÇÕES ETC DENTRO DO SISTEMA, PARA QUE ELE NÃO PRECISE ME ENCHER O SACO PEDINDO PERMISSÃO O TEMPO TODO

QUANTO AO SEO, EU ESQUECI DE TE PASSAR A COMPLEXIDADE DO QUE EU CRIEI

1. Varredura, Auditoria e Injeção de Tags (optimize_site_seo_and_virality)
Esta ferramenta varre um diretório buscando todos os arquivos .html para auditar e injetar metadados críticos na tag <head> se estiverem ausentes:

Auditoria de Presença: O script verifica se a página possui:
Título semântico principal (<h1>).
Meta tag de descrição (name="description").
Tags Open Graph (property="og:").
Esquema de Dados Estruturados (type="application/ld+json").
Injeção Automática de Open Graph (OG): Se não encontrar tags de OG, ele injeta no <head>:
html
<meta property="og:title" content="{business_name}" />
<meta property="og:description" content="Saiba mais sobre {business_name}!" />
<meta property="og:type" content="website" />
Injeção de Schema JSON-LD (LocalBusiness): Ele gera e injeta um bloco de dados estruturados para que o Google entenda a entidade comercial:
html
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "LocalBusiness",
  "name": "[Nome do Negócio]",
  "url": "https://[nomedonegociolimpo].com.br",
  "logo": "https://[nomedonegociolimpo].com.br/logo.png",
  "description": "Site otimizado do [Nome do Negócio] com melhores práticas de SEO e conversão viral.",
  "currenciesAccepted": "BRL",
  "paymentAccepted": "Cash, Credit Card"
}
</script>
2. Otimização de Meta Keywords e Descriptions (optimize_site_keywords_and_meta)
Focado diretamente nos arquivos index.html e mobile.html de um diretório:

Palavras-chave (meta name="keywords"): Procura a tag de keywords existente. Se a nova palavra-chave recomendada não estiver lá, ele faz o append preservando as palavras antigas:
Lógica: f"{nova_keyword}, {keywords_existentes}".
Descrição (meta name="description"): Substitui o conteúdo inteiro da tag pela nova descrição otimizada.
3. Densidade de Palavras-Chave (check_seo_keywords)
Calcula estatísticas de densidade no texto para evitar penalizações de keyword stuffing no Google:

seo_score: Porcentagem das palavras-chave fornecidas que aparecem pelo menos uma vez no texto.
coverage: Lista contendo a contagem exata de aparições de cada palavra-chave específica.
density: Proporção matemática de palavras-chave encontradas em relação ao total de palavras no texto (total_encontradas / total_palavras).
4. O Loop de SEO do Agente Autônomo (run_vivar_seo_agent_loop)
Quando o loop autônomo (Vivar) cria um post, ele gera metadados específicos para alimentar a estrutura de SEO automaticamente:


Gera o artigo e extrai:
keyword: Palavra-chave SEO mais relevante.
excerpt: Resumo de 2 frases para SEO (máximo 150 caracteres).
Chama a função optimize_site_keywords_and_meta aplicando essas informações diretamente ao site.

Gera o artigo.
Faz duas chamadas complementares ao LLM para criar os metadados:
Prompt do Excerpt: "Gere uma descrição meta de SEO (resumo de 2 frases, máximo 150 caracteres) sobre este título: {title}"
Prompt das Keywords: "Gere 3 palavras-chave de SEO separadas por vírgula para o título: {title}"
Chama a função optimize_site_keywords_and_meta aplicando a descrição e as keywords no HTML das páginas públicas.

precisa de um agente tbm de briefing e que vai conversar com o usuario pedindo todo necessario, acessos, chaves, api, etc etc etc


vai agora sim, vai gera tudo de novo, vc consegue
sem preguiça, sem resumo, sem medicridade
completo, complexo, inteiro, e perfeccionista

AHHHHH, QUERO BLINDAR MEU MPC PARA QUE NINGUEM, A NAO SER EU CONSIGA VER O CODIGO DELE, VAI QUE FICA MUITO BOM E ALGUEM QUER ROUBAR


---

### 👤 USER:
deixa te falar um negocio que eu esqueci...rsrs
o gerador de mcp, serve tbm para fazer outras coisas como um agente de programação, um agente de marketing tbm, uma vez que eu ja configurei

eu quero 2 prgramadores senior que é para nao da problema

outro agente especializado em duplicar o sistema/site/projeto/sei lá o que: ele sempre faz duas versões do que está sendo feito... uma para pc, outra para mobbile
enche o saco de todo mundo em cada fase para sempre ter o mobile e o desktop

DE A ELE TODAS AS PERMISSÕES, AUTORIZAÇÕES ETC DENTRO DO SISTEMA, PARA QUE ELE NÃO PRECISE ME ENCHER O SACO PEDINDO PERMISSÃO O TEMPO TODO

QUANTO AO SEO, EU ESQUECI DE TE PASSAR A COMPLEXIDADE DO QUE EU CRIEI

1. Varredura, Auditoria e Injeção de Tags (optimize_site_seo_and_virality)
Esta ferramenta varre um diretório buscando todos os arquivos .html para auditar e injetar metadados críticos na tag <head> se estiverem ausentes:

Auditoria de Presença: O script verifica se a página possui:
Título semântico principal (<h1>).
Meta tag de descrição (name="description").
Tags Open Graph (property="og:").
Esquema de Dados Estruturados (type="application/ld+json").
Injeção Automática de Open Graph (OG): Se não encontrar tags de OG, ele injeta no <head>:
html
<meta property="og:title" content="{business_name}" />
<meta property="og:description" content="Saiba mais sobre {business_name}!" />
<meta property="og:type" content="website" />
Injeção de Schema JSON-LD (LocalBusiness): Ele gera e injeta um bloco de dados estruturados para que o Google entenda a entidade comercial:
html
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "LocalBusiness",
  "name": "[Nome do Negócio]",
  "url": "https://[nomedonegociolimpo].com.br",
  "logo": "https://[nomedonegociolimpo].com.br/logo.png",
  "description": "Site otimizado do [Nome do Negócio] com melhores práticas de SEO e conversão viral.",
  "currenciesAccepted": "BRL",
  "paymentAccepted": "Cash, Credit Card"
}
</script>
2. Otimização de Meta Keywords e Descriptions (optimize_site_keywords_and_meta)
Focado diretamente nos arquivos index.html e mobile.html de um diretório:

Palavras-chave (meta name="keywords"): Procura a tag de keywords existente. Se a nova palavra-chave recomendada não estiver lá, ele faz o append preservando as palavras antigas:
Lógica: f"{nova_keyword}, {keywords_existentes}".
Descrição (meta name="description"): Substitui o conteúdo inteiro da tag pela nova descrição otimizada.
3. Densidade de Palavras-Chave (check_seo_keywords)
Calcula estatísticas de densidade no texto para evitar penalizações de keyword stuffing no Google:

seo_score: Porcentagem das palavras-chave fornecidas que aparecem pelo menos uma vez no texto.
coverage: Lista contendo a contagem exata de aparições de cada palavra-chave específica.
density: Proporção matemática de palavras-chave encontradas em relação ao total de palavras no texto (total_encontradas / total_palavras).
4. O Loop de SEO do Agente Autônomo (run_vivar_seo_agent_loop)
Quando o loop autônomo (Vivar) cria um post, ele gera metadados específicos para alimentar a estrutura de SEO automaticamente:


Gera o artigo e extrai:
keyword: Palavra-chave SEO mais relevante.
excerpt: Resumo de 2 frases para SEO (máximo 150 caracteres).
Chama a função optimize_site_keywords_and_meta aplicando essas informações diretamente ao site.

Gera o artigo.
Faz duas chamadas complementares ao LLM para criar os metadados:
Prompt do Excerpt: "Gere uma descrição meta de SEO (resumo de 2 frases, máximo 150 caracteres) sobre este título: {title}"
Prompt das Keywords: "Gere 3 palavras-chave de SEO separadas por vírgula para o título: {title}"
Chama a função optimize_site_keywords_and_meta aplicando a descrição e as keywords no HTML das páginas públicas.

precisa de um agente tbm de briefing e que vai conversar com o usuario pedindo todo necessario, acessos, chaves, api, etc etc etc


outro agente importante que vai passar por todas as fases, antes de cada fase na realidade é o gestor de token:
# Sistema de Economia e Rodízio de APIs (Antimatter Squad)

Aqui está a implementação do sistema de controle de tokens e roteamento multi-provedor (OpenRouter, Groq e HuggingFace) do Antimatter Squad. Este sistema evita o consumo de tokens locais do host (Antigravity/Antimatter) para tarefas geradoras e faz fallback automático em caso de rate limit ou falha de conexão.

---

## 1. Configuração de Variáveis de Ambiente (`.env.antimatter`)

As chaves dos provedores externos são configuradas em ordem de prioridade. Se o OpenRouter falhar ou atingir o limite diário, o sistema faz fallback para o Groq, e depois para o HuggingFace.

[Ver arquivo completo](file:///C:/Users/User/qwen-coding-engine/.env.antimatter)
```ini
# Provedores externos (Prioridade: OpenRouter → Groq → HuggingFace)
OPENROUTER_API_KEY=your_openrouter_api_key_here
GROQ_API_KEY=your_groq_api_key_here
HUGGINGFACE_API_KEY=your_huggingface_api_key_here

# Diretório de Trabalho do Workspace
ANTIMATTER_WORKSPACE=C:\Users\User\.gemini\antigravity-ide\scratch\Marketeiro

# Modo Autônomo e Controle de Execução
MCP_AUTO_APPROVE=true
AGENT_MODE=autonomous
PYTHONUNBUFFERED=1

# Orçamento de Tokens e Cache local
ANTIMATTER_TOKEN_BUDGET=45000
ANTIMATTER_CACHE_ENABLED=true
ANTIMATTER_CHECKPOINT_ENABLED=true

# Limites e Timeout
ANTIMATTER_MAX_RETRIES=3
ANTIMATTER_TIMEOUT_SEC=120
```

---

## 2. Política de Isolamento de Tokens (`token_policy.py`)

A classe `TokenPolicyController` garante que requisições geradoras de conteúdo (que gastam muitos tokens) não usem os tokens locais do host. Se houver tentativa de usar o host para geração de conteúdo, uma exceção é disparada.

[Ver código original](file:///C:/Users/User/qwen-coding-engine/src/antimatter_squad/token_policy.py)
```python
"""
Antimatter Squad — Token Isolation Policy Enforcer (Anti-Vampire Clause)
Enforces strict routing rules to prevent hosting provider (Antigravity) token consumption.
"""
from __future__ import annotations

import os
import time
import json
from pathlib import Path
from typing import Dict, Any, List

# Policy violation logs path
VIOLATIONS_LOG_PATH = Path("logs/token_policy_violations.jsonl")


class TokenPolicyViolation(Exception):
    """Raised when the orchestrator attempts to consume host tokens for content generation."""
    pass


class TokenPolicyController:
    def __init__(self):
        self.host_native_calls = 0
        self.external_api_calls = 0
        self.violation_count = 0
        self.call_history: List[Dict[str, Any]] = []

    def is_strict(self) -> bool:
        return os.getenv("TOKEN_POLICY_STRICT", "true").lower() == "true"

    def is_host_blocked(self) -> bool:
        return os.getenv("HOST_NATIVE_BLOCKED", "true").lower() == "true"

    def is_external_required(self) -> bool:
        return os.getenv("REQUIRE_EXTERNAL_API", "true").lower() == "true"

    def validate_keys_configured(self) -> None:
        """Verify that at least one external provider API key is present."""
        if not self.is_external_required():
            return
        
        has_keys = (
            bool(os.getenv("OPENROUTER_API_KEY"))
            or bool(os.getenv("GROQ_API_KEY"))
            or bool(os.getenv("HUGGINGFACE_API_KEY"))
        )
        if not has_keys:
            raise TokenPolicyViolation(
                "❌ Configuração crítica: Nenhuma API externa configurada. O Antimatter Core requer pelo menos "
                "uma chave de API externa para operar sem consumir tokens do host. Adicione "
                "OPENROUTER_API_KEY, GROQ_API_KEY ou HUGGINGFACE_API_KEY ao seu .env."
            )

    def log_dispatch(self, role: str, expected_tokens: int) -> None:
        """Register the dispatch of an agent request."""
        event = {
            "event": "agent_dispatch",
            "role": role,
            "expected_tokens": expected_tokens,
            "source": "external",
            "timestamp": time.time(),
        }
        self.call_history.append(event)

    def log_completion(self, provider: str, tokens_used: int, role: str) -> None:
        """Register completion of an agent call and audit for violations."""
        event = {
            "event": "agent_complete",
            "tokens_used": tokens_used,
            "provider": provider,
            "role": role,
            "timestamp": time.time(),
        }
        self.call_history.append(event)

        # Check if the provider is host native for generative roles
        is_generative_role = role not in ("concierge", "system")
        is_host = provider == "host_native" or provider == "antigravity"

        if is_host:
            self.host_native_calls += 1
            if is_generative_role and (self.is_host_blocked() or self.is_strict()):
                self.violation_count += 1
                self.record_violation(role, f"Generative action routed to {provider}")
                raise TokenPolicyViolation(
                    "⚠️ Erro de configuração: o sistema tentou usar tokens do Antigravity para gerar conteúdo. "
                    "Verifique se as APIs externas estão configuradas corretamente."
                )
        else:
            self.external_api_calls += 1

        # Run audit loop every 5 calls
        if len(self.call_history) % 5 == 0:
            self.run_self_audit()

    def record_violation(self, role: str, action: str) -> None:
        """Persist violation details to the JSONL log file."""
        VIOLATIONS_LOG_PATH.parent.mkdir(parents=True, exist_ok=True)
        violation_entry = {
            "violation": {
                "timestamp": time.strftime("%Y-%m-%dT%H:%M:%SZ", time.gmtime()),
                "role": role,
                "attempted_action": action,
                "blocked": True,
                "corrective_action": "check_api_keys",
            }
        }
        with open(VIOLATIONS_LOG_PATH, "a", encoding="utf-8") as f:
            f.write(json.dumps(violation_entry) + "\n")

    def run_self_audit(self) -> None:
        """Audit the recent calls for host-native consumption percentage."""
        total_calls = self.host_native_calls + self.external_api_calls
        if total_calls == 0:
            return
        
        ratio = self.host_native_calls / total_calls
        if ratio > 0.10:
            print(f"[WARNING] Token Policy Audit warning: host_native calls make up {ratio*100:.1f}% of total calls.")

        if self.violation_count >= 3:
            raise TokenPolicyViolation(
                "❌ Proteção ativada: Mais de 3 violações de política de tokens nas últimas chamadas. "
                "Execução pausada por segurança."
            )

    def get_token_usage_summary(self) -> Dict[str, Any]:
        """Summary of host vs external token usage."""
        return {
            "host_tokens_used": self.host_native_calls * 1000,  # rough estimate
            "external_api_calls": self.external_api_calls,
            "estimated_external_cost_usd": self.external_api_calls * 0.0001,  # estimate
            "policy_compliant": self.host_native_calls == 0,
        }

    def force_external_only_test(self, role: str) -> Dict[str, Any]:
        """Perform a test routing check for a role."""
        self.validate_keys_configured()
        configured = []
        if os.getenv("OPENROUTER_API_KEY"):
            configured.append("openrouter")
        if os.getenv("GROQ_API_KEY"):
            configured.append("groq")
        if os.getenv("HUGGINGFACE_API_KEY"):
            configured.append("huggingface")

        return {
            "policy_status": "compliant",
            "configured_providers": configured,
            "host_native_blocked": self.is_host_blocked(),
            "test_call_result": {"routed_to": configured[0] if configured else "none", "status": "success"},
            "concierge_summary": "✅ Política de tokens validada: o sistema está configurado para usar APENAS APIs externas. Tokens do Antigravity preservados para orquestração e interface."
        }


# Global instance of the controller
policy_controller = TokenPolicyController()
```

---

## 3. Roteador de Provedores e Rodízio (`provider_router.py`)

A classe `ProviderRouter` inicializa os clientes para cada provedor configurado e gerencia o envio de mensagens para o modelo associado ao papel/função ativa. Ela monitora o estado de saúde de cada API, detecta falhas (como erros 429 - rate limit) e desvia o tráfego para a próxima API da fila.

[Ver código original](file:///C:/Users/User/qwen-coding-engine/src/antimatter_squad/provider_router.py)
```python
"""
Antimatter Squad — Provider Router + Model Registry
Multi-provider (OpenRouter / Groq / HuggingFace) with health check,
automatic fallback, and budget-aware routing.
"""
from __future__ import annotations

import asyncio
import logging
import os
import time
from dataclasses import dataclass, field
from typing import Any, Dict, List, Optional, Tuple

from openai import AsyncOpenAI

logger = logging.getLogger("antimatter.router")


# ═══════════════════════════════════════════════════════
# Model Configuration per Role
# ═══════════════════════════════════════════════════════

@dataclass
class RoleConfig:
    temperature: float
    max_tokens: int


ROLE_DEFAULTS: Dict[str, RoleConfig] = {
    "planner_a":  RoleConfig(temperature=0.6, max_tokens=3072),
    "planner_b":  RoleConfig(temperature=0.7, max_tokens=3072),
    "programmer": RoleConfig(temperature=0.2, max_tokens=4096),
    "auditor":    RoleConfig(temperature=0.3, max_tokens=4096),
    "reviewer_e": RoleConfig(temperature=0.5, max_tokens=3072),
    "reviewer_f": RoleConfig(temperature=0.5, max_tokens=3072),
    "tester":     RoleConfig(temperature=0.4, max_tokens=3072),
    "corrector":  RoleConfig(temperature=0.2, max_tokens=4096),
}


# ═══════════════════════════════════════════════════════
# Provider Configurations
# ═══════════════════════════════════════════════════════

@dataclass
class ProviderConfig:
    name: str
    base_url: str
    api_key: str
    models: Dict[str, str]
    priority: int
    healthy: bool = True
    last_check: float = 0.0
    consecutive_failures: int = 0
    daily_request_count: int = 0
    daily_request_limit: int = 200
    extra_headers: Dict[str, str] = field(default_factory=dict)


def load_providers() -> List[ProviderConfig]:
    """Load provider configs from environment variables."""
    providers = []

    # 1. OpenRouter (primary) — models verified against /api/v1/models free tier
    or_key = os.getenv("OPENROUTER_API_KEY", "")
    if or_key:
        providers.append(ProviderConfig(
            name="openrouter",
            base_url="https://openrouter.ai/api/v1",
            api_key=or_key,
            priority=1,
            daily_request_limit=200,
            models={
                "planner_a":  "meta-llama/llama-3.3-70b-instruct:free",
                "planner_b":  "google/gemma-4-31b-it:free",
                "programmer": "deepseek/deepseek-v4-flash:free",
                "auditor":    "deepseek/deepseek-v4-flash:free",
                "reviewer_e": "nousresearch/hermes-3-llama-3.1-405b:free",
                "reviewer_f": "qwen/qwen3-next-80b-a3b-instruct:free",
                "tester":     "meta-llama/llama-3.2-3b-instruct:free",
                "corrector":  "qwen/qwen3-coder:free",
            },
            extra_headers={
                "HTTP-Referer": "https://github.com/antimatter-squad",
                "X-Title": "Antimatter Squad Orchestrator",
            },
        ))

    # 2. Groq (fallback 1)
    groq_key = os.getenv("GROQ_API_KEY", "")
    if groq_key:
        providers.append(ProviderConfig(
            name="groq",
            base_url="https://api.groq.com/openai/v1",
            api_key=groq_key,
            priority=2,
            daily_request_limit=500,
            models={
                "planner_a":  "llama-3.1-8b-instant",
                "planner_b":  "llama-3.3-70b-versatile",
                "programmer": "qwen-qwq-32b",
                "auditor":    "qwen-qwq-32b",
                "reviewer_e": "llama-3.1-8b-instant",
                "reviewer_f": "llama-3.3-70b-versatile",
                "tester":     "llama-3.1-8b-instant",
                "corrector":  "qwen-qwq-32b",
            },
        ))

    # 3. HuggingFace (fallback 2)
    hf_key = os.getenv("HUGGINGFACE_API_KEY", "")
    if hf_key:
        providers.append(ProviderConfig(
            name="huggingface",
            base_url="https://router.huggingface.co/v1",
            api_key=hf_key,
            priority=3,
            daily_request_limit=300,
            models={
                "planner_a":  "Qwen/Qwen2.5-Coder-32B-Instruct",
                "planner_b":  "meta-llama/Meta-Llama-3.1-8B-Instruct",
                "programmer": "Qwen/Qwen2.5-Coder-32B-Instruct",
                "auditor":    "Qwen/Qwen2.5-Coder-32B-Instruct",
                "reviewer_e": "Qwen/Qwen2.5-Coder-32B-Instruct",
                "reviewer_f": "meta-llama/Meta-Llama-3.1-8B-Instruct",
                "tester":     "Qwen/Qwen2.5-Coder-32B-Instruct",
                "corrector":  "Qwen/Qwen2.5-Coder-32B-Instruct",
            },
        ))

    providers.sort(key=lambda p: p.priority)
    return providers


class ProviderRouter:
    """
    Routes agent calls to the best available provider.
    Tracks health, budget, rate limits, and auto-fallback.
    """

    def __init__(self, providers: Optional[List[ProviderConfig]] = None):
        self.providers = providers or load_providers()
        self._clients: Dict[str, AsyncOpenAI] = {}
        self._health_cache_ttl = 60  # seconds

        if not self.providers:
            raise RuntimeError(
                "No AI providers configured. Set at least OPENROUTER_API_KEY "
                "in environment variables."
            )

        logger.info(
            f"ProviderRouter initialized with {len(self.providers)} providers: "
            f"{[p.name for p in self.providers]}"
        )

    def _get_client(self, provider: ProviderConfig) -> AsyncOpenAI:
        """Get or create an AsyncOpenAI client for a provider."""
        if provider.name not in self._clients:
            self._clients[provider.name] = AsyncOpenAI(
                api_key=provider.api_key,
                base_url=provider.base_url,
                max_retries=0,  # We handle retries ourselves
                default_headers=provider.extra_headers or None,
            )
        return self._clients[provider.name]

    async def health_check(self, provider: ProviderConfig) -> bool:
        """
        Lightweight health check — tries a minimal API call.
        Caches result for _health_cache_ttl seconds.
        """
        now = time.time()
        if now - provider.last_check < self._health_cache_ttl:
            return provider.healthy

        try:
            client = self._get_client(provider)
            # Minimal completion call to test connectivity
            first_model = list(provider.models.values())[0]
            resp = await asyncio.wait_for(
                client.chat.completions.create(
                    model=first_model,
                    messages=[{"role": "user", "content": "ping"}],
                    max_tokens=5,
                    temperature=0,
                ),
                timeout=15,
            )
            provider.healthy = True
            provider.consecutive_failures = 0
            logger.info(f"Health check PASS: {provider.name}")
        except Exception as e:
            error_str = str(e).lower()
            # 429 = rate limited but reachable — still healthy
            if "429" in error_str or "rate" in error_str:
                provider.healthy = True
                provider.consecutive_failures = 0
                logger.info(f"Health check PASS (rate-limited): {provider.name}")
            else:
                provider.healthy = False
                provider.consecutive_failures += 1
                logger.warning(f"Health check FAIL: {provider.name}: {e}")

        provider.last_check = now
        return provider.healthy

    async def health_check_all(self) -> Dict[str, bool]:
        """Run health checks on all providers in parallel."""
        results = {}
        tasks = []
        for p in self.providers:
            tasks.append(self.health_check(p))

        checks = await asyncio.gather(*tasks, return_exceptions=True)
        for p, result in zip(self.providers, checks):
            results[p.name] = result if isinstance(result, bool) else False
        return results

    def get_best_provider(
        self, role: str, exclude_names: Optional[List[str]] = None
    ) -> Tuple[ProviderConfig, str, AsyncOpenAI]:
        """
        Returns (provider, model_id, client) for the given role.
        Falls back through providers sorted by priority, skipping unhealthy ones or excluded ones.
        """
        exclude = exclude_names or []
        for provider in self.providers:
            if provider.name in exclude:
                continue
            if not provider.healthy:
                # Auto-recover unhealthy providers after 30 seconds to handle temporary rate limits
                if time.time() - provider.last_check > 30:
                    provider.healthy = True
                    provider.consecutive_failures = 0
                    logger.info(f"Provider {provider.name} auto-recovered from UNHEALTHY state")
                else:
                    continue
            if provider.daily_request_count >= provider.daily_request_limit:
                logger.warning(
                    f"Provider {provider.name} at daily limit "
                    f"({provider.daily_request_count}/{provider.daily_request_limit})"
                )
                continue
            model_id = provider.models.get(role)
            if model_id:
                client = self._get_client(provider)
                return provider, model_id, client

        # All providers exhausted — force first provider that is not excluded
        available = [p for p in self.providers if p.name not in exclude]
        p = available[0] if available else self.providers[0]
        model_id = p.models.get(role, list(p.models.values())[0])
        logger.error(f"All providers exhausted for role={role}, forcing {p.name}/{model_id}")
        return p, model_id, self._get_client(p)

    def mark_failure(self, provider: ProviderConfig) -> None:
        """Mark a provider as having a failure (for rate limit or error)."""
        provider.consecutive_failures += 1
        provider.last_check = time.time()
        if provider.consecutive_failures >= 3:
            provider.healthy = False
            logger.warning(
                f"Provider {provider.name} marked UNHEALTHY after "
                f"{provider.consecutive_failures} consecutive failures"
            )

    def mark_success(self, provider: ProviderConfig) -> None:
        """Mark a successful call."""
        provider.consecutive_failures = 0
        provider.daily_request_count += 1

    def budget_status(self) -> Dict[str, Any]:
        """Returns budget/usage status for all providers."""
        return {
            p.name: {
                "healthy": p.healthy,
                "requests_used": p.daily_request_count,
                "requests_limit": p.daily_request_limit,
                "pct_used": round(
                    p.daily_request_count / max(1, p.daily_request_limit) * 100, 1
                ),
                "consecutive_failures": p.consecutive_failures,
            }
            for p in self.providers
        }

    def reset_daily_counts(self) -> None:
        """Reset daily counters (call at midnight or start of new pipeline batch)."""
        for p in self.providers:
            p.daily_request_count = 0
            p.healthy = True
            p.consecutive_failures = 0
```
além dessas apis basicas que utilizamos, bora colocar mais duas, para sempre garantir que vai dar certo, daí tu escolhe e me pede quem... free.. qual api vai querer e tal

vai agora sim, vai gera tudo de novo, vc consegue
sem preguiça, sem resumo, sem medicridade
completo, complexo, inteiro, e perfeccionista



AHHHHH, QUERO BLINDAR MEU MPC PARA QUE NINGUEM, A NAO SER EU CONSIGA VER O CODIGO DELE, VAI QUE FICA MUITO BOM E ALGUEM QUER ROUBAR


---

### 👤 USER:
isso que vc me deu agora é ele completo ou só as novas partes que eu te dei??

ja posso pegar ele e colar direto no antoigravity para criar essa mcp? ou tem que copiar uma parte depois outra??

ou vc já me deu ele totalmente completo? copia e cola?

nome dele será Doutor, em homenagem ao Doutor who

Se ainda não ta completo, do 0 ao 100%
me avisa que ja te colo as apis

mas se ja tiver completo, sem problemas, eu coloco as apis la no antigravity


---

### 👤 USER:
eu quero completo, do 0 ao 100
E add nele tbm, a estrutura de pastas a serem criadas, o que vai em cada pasta, etc... ele consegue

api eu boto no final

Me da do 0, desde a ideia até a ultima linha e ideia que fizemos


VAi
primeiro toda a estrutura de pastas
depois a comunicação do que é e do que faz
a logica de como ele vai agir

tudo tudo tudo num arquivo só

daí o codigo
e por fim sei lá o que

vai, vai dar certo

Bora

COmpleto, complexo, perfeito

Sem preguiça, sem resumo, sem medicridade


---

### 👤 USER:
quais as apis mesmo?


---

### 👤 USER:
nao nao, quais apis eu tenho de ir buscar, é disso que eu to falando
nao o que cada uma faz, quais eu vou buscar?


---

### 👤 USER:
together ta pedindo para pagar, ja tenho uma api feita, mas eu nao consigo pegar ela, e para fazer uma nova ta pedindo para pagar


---

### 👤 USER:
uma api geral do githyb ajudaria ou nao faz diferença?


---

### 👤 USER:

organiza e faz um prompt para ele entender as apis, e substitui aquela que na teve
e orienta sobre as que não estavam no escopo como google (gerar imagem, conversar voz, criar slide, tabela, doc, pdf, etc.. fazer consulta nos sistemas google como google trend, etc...)

openrouter:  your_openrouter_api_key_here

groq: your_groq_api_key_here

huggiface: your_huggingface_api_key_here

serper: your_serper_api_key_here

cloudinary: your_cloudinary_api_key_here

fireworks: your_fireworks_api_key_here

twllio: your_twilio_sid_here
your_twilio_auth_token_here

google: your_google_api_key_here

github: your_github_pat_here


---

### 👤 USER:
sim acabei tudo

Agora faça um mega prompt onde concatena tudo explica como usar tudo como fazer tudo e principalmente, como transformar isso numa mcp


---
